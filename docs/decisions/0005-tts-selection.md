# ADR-0005：M3 语音输出（TTS）选型

> ## 🔄 状态更新（2026-09-14）——TTS 实际落点已定，但**本文正文仍是当时的历史判断**
>
> **实际落点是 `mlx-community/VoxCPM2-4bit`（经 `mlx-audio`，48 kHz），
> 走 `services/tts` 的 HTTP 契约**（`POST /speak {text,lang}` → `audio/wav`）。
> `services/tts/server.py --backend voxcpm2` **是默认后端**；
> macOS `say`（`--backend say`）**降级为保底**——零依赖、零下载，但语音质量与
> 情感都不在这个方案的验收范围内。Rust 侧经 `src/talk.rs::HttpTts`（`tts_url`，
> 默认 `127.0.0.1:8765`）与 `SayTts` 兜底接上，**默认关**（`talk_enabled`）。
>
> **实测（2026-09-14，M1 Max / 64 GB）**：48 kHz 单声道 16bit；模型加载 0.86s；
> HTTP 端到端一句 **2.6–3.9s**；进程峰值 RSS **约 2.5 GB**。
> 明细见 [`docs/benchmarks-talk.md`](../benchmarks-talk.md)（**MLX 4bit 路径**，
> 与 `benchmarks-m3.md` 的 **speech-swift bf16 路径**不是同一条，数字不能混引）。
> 三语端到端都跑通，泰语回环转写（whisper th）得到
> 「วันอีกที่เชียงใหม่อากาศดีอุณหภูมิ 32 องศา」，即**说的确实是泰语**。
>
> ⚠️ **这份更新能关掉什么、不能关掉什么**：
> - ✅ 关掉了本文 §5 的第 5 条（资源账）：**TTS 边车与通话 LLM 边车同时常驻**，
>   实测 TTS 进程峰值约 2.5 GB + LLM 常驻峰值 1.59 GB ≈ 4.1 GB，
>   在 ADR-0002 的 ≤9 GiB 之内（`scripts/talk-e2e.sh` 就是这么跑的）。
> - ❌ **没有关掉 §5 的第 1 条（Q3 人耳验收）**：本轮只证明「能出声、说的是目标语言」，
>   **音色、情感、方言一个字都没评**。「像真人有感情」这个要求**声学指标验证不了**。
> - ❌ **没有关掉方言那一格**：VoxCPM2 的 9 种中文方言能否靠 instruct 触发，
>   **仍然没有客观判据**（`language-id` 只到语言级）。
> - ❌ **4bit 与 bf16 的质量对比没做**（只测了 4bit 能跑、时延与内存）。
>   ⚠️ `benchmarks-m3.md` §6 那批 VoxCPM2 数据是 **speech-swift 的 bf16 路径**，
>   与本次的 **MLX 4bit 路径不是同一个运行时，不要混着引用**。
> - ❌ **本文 §3 的情形 A / B（闽南语判定）与 §4 的三条走向，一条都没被本轮影响。**
>   Q3 仍开着，本文正文按原样保留作历史。
>
> > **「VoxCPM2 已选定」这句话的准确边界**：**作为 TTS 的实现落点是定了**
> > （因为它仍是唯一能说泰语的候选，且已在目标机器上跑通），
> > **但「语音质量达标」没有定**——那要人耳。

- **状态**：**草稿，不可据此拍板**（2026-09-03）
- **卡在一件只有 jason 能做的事**：`~/Desktop/agentear-tts-samples/` 里那三段音频，
  `nan_tw_000.wav` 到底是**闽南语**还是**用普通话腔调念的闽南语汉字**。
  这一条决定下面几乎所有结论的走向，而它需要人耳（`progress.md` 的 Q3）。
- **已经确定的部分**：
  1. **载体通了** —— `OmniVoice` 经 `mlx-audio` 在 M1 Max 上能跑，
     热启动 3–4 秒一句，峰值 RSS 1.8 GB
  2. **许可最干净的 Qwen3-TTS 不覆盖需求** —— Apache-2.0，但公布的中文方言
     只有北京话和四川话
  3. **台湾闽南语 ≠ 新加坡闽南语** —— MERaLiON 那个模型卡明写 Singapore Hokkien
  4. **所有候选都是 PyTorch/CUDA 取向，没有单二进制** —— TTS 只能走
     ADR-0002 允许的「独立进程边车」路线，是新增一整套运行时
- **明确不做的事**：**本文不选定方案**。选型是产品决策，而最关键的证据
  （方言到底对不对）还没有。
- **实测依据**：`docs/tts-survey.md` 的候选表与「实测」一节

---

## 1. 背景与约束

`docs/milestones.md` 的 M3 要「语音输出与打断」。jason 2026-09-02 补了一条
以前不在任何文档里的约束：

> **输出语音要能说方言**：台湾话、广东话、香港话、英语、泰语。
> 允许分阶段——先做普通话或先做英语都行。

加上全局的隐私红线：**任何需要把文本或音频送第三方服务器的方案直接出局**。

⚠️ **方向不要混**：这里说的是**输出侧**（让它说方言）。输入侧（听懂方言）
是另一件事——现有 SenseVoice 覆盖 zh/en/yue/ja/ko，**粤语能听、闽南语听不了**，
那个缺口记在 `docs/milestones.md`，需要单独拍板。

## 2. 候选与已知事实

完整表在 `docs/tts-survey.md`。这里只列对决策有分量的：

| 候选 | 方言覆盖 | Apple Silicon | 许可 |
|---|---|---|---|
| Qwen3-TTS | 仅北京话、四川话 | ✅（via mlx-audio） | **Apache-2.0** |
| OmniVoice（`mlx-community/OmniVoice-bf16`） | 声称 646+ 语言，**实际方言表现未验证** | ✅ **已实测跑通** | ⚠️ **无模型卡、无许可标注** |
| MERaLiON-OmniVoice-Hokkien | 新加坡闽南语 | ❓ 未测（示例是 CUDA） | 自定义 `meralion-3-public-licence` |
| BreezyVoice-Taigi | **台湾闽南语** | ❓ 未测 | ❓ 未查 |

### 2.1 实测数据（T3.1.2）

| 输入 | 耗时（热） | 峰值 RSS |
|---|---|---|
| 普通话 | 4 s | 1832 MB |
| 闽南语汉字 | 3 s | 1835 MB |
| 粤语汉字 | 4 s | 1831 MB |

**这三行只证明「能出声、快、内存可控」**，不证明说的是方言——
CLI 输出还显示 `Language: en`、`Voice: None`，语言参数似乎没生效。

## 3. 决策要卡在哪一步，取决于 Q3 的答案

### 情形 A：`nan_tw_000.wav` 确实是闽南语

那么 **OmniVoice 是最有希望的单一候选**：一个模型覆盖多语言、已在目标机器上
跑通、速度和内存都可接受。

接下来要做的（不在本文范围）：
1. **查清许可** —— `mlx-community/OmniVoice-bf16` 没有模型卡也没有许可标注，
   而我们要随包分发或按需下载。上游是谁、什么许可，必须查到白纸黑字。
   （泰语模型那次的经验：MIT 最省事，Apache-2.0 要看 NOTICE，
   自定义许可要逐条读。）
2. **测方言质量** —— 「能说」和「说得像」是两件事，要母语者判断。
3. **测语言/音色的控制方式** —— CLI 看着不支持，可能要走 Python API。

### 情形 B：`nan_tw_000.wav` 是普通话念汉字

那么 OmniVoice 的「646+ 语言」不包含闽南语，**要么换模型，要么放弃方言**。

下一步是验证 `MERaLiON-OmniVoice-Hokkien` 与 `mlx-community/OmniVoice-bf16`
**是否同源**（两者都叫 OmniVoice、都有 `audio_tokenizer` 子目录，架构上很可能
是同一个基座）。若同源，那份闽南语微调权重理论上可以转成 MLX 格式再用
mlx-audio 跑——**和泰语 whisper 那次是同一套路数**（转换 + 量化 + 校验指纹）。

⚠️ 但那是**新加坡闽南语**，不是台湾话。台湾话对口的是 BreezyVoice-Taigi，
而它能不能在 Apple Silicon 上跑完全没测。

## 4. 如果闽南语最终没有可用方案，M3 的三条走向

**这一节是给 jason 拍板用的，本文不替他选。** 每条都列了代价。

### 走向一：只做普通话 + 英语，方言推迟

- **代价**：方言这条约束落空。但 jason 自己说过「先完成普通话或先完成英语的都可以」，
  所以这不违背他的意图，只是把方言从 M3 挪到更后面。
- **好处**：M3 立刻可做。Qwen3-TTS（Apache-2.0、许可最干净、已在 mlx-audio 上）
  就够用，连许可都不用纠结。
- **风险**：如果方言是真需求，推迟等于一直不做——因为没有触发它的时间点。

### 走向二：推迟整个 M3，先做 M2b / M3.5

- **代价**：agent 的「嘴」一直没有。而 jason 明确说过 AgentEar 是
  「agent 的耳朵和嘴巴」，只有耳朵是半个产品。
- **好处**：M3.5（录音笔刻画）本来就是「到手即做、不排队」，
  而那台机器还没到手；M2b（知识库投递）也还没做。不缺活干。
- **风险**：TTS 的未知不会因为推迟而变少。

### 走向三：接受「TTS 只做普通话，但方言用别的路子」

比如：方言内容仍然输出**文字**，只有普通话/英语走语音。

- **代价**：体验不一致，用户要记住「哪些语言会读出来」。
- **好处**：不阻塞 M3 主体（AEC、打断式半双工才是 M3 真正的工程量），
  方言可以在 TTS 侧有方案时再补上。
- **风险**：这是个折中，做完可能两边不讨好。

## 5. 还没查清的（拍板前应该补的）

1. **Q3：那三段音频到底是不是方言**（阻塞一切）
2. `mlx-community/OmniVoice-bf16` 的上游与许可
3. MERaLiON-Hokkien 与它是否同源
4. BreezyVoice-Taigi 能否在 Apple Silicon 上跑
5. **资源账**：TTS 峰值 1.8 GB + M2 常驻 LLM 7.3 GB ≈ 9.1 GB，
   已经贴着 ADR-0002 的 ≤9 GiB 上限。两者能否同时常驻，
   还是要做按需加载/卸载——这个问题**与选哪个模型无关，一定要回答**。
6. **AEC 仍然是 M3 最大的工程量**，和 TTS 选型是两件独立的事
   （`docs/asr-selection.md` §4 已经把阈值列好了）。选型卡住不代表 M3 卡住，
   AEC 那块随时可以开始。
