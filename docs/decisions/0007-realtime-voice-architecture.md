# ADR-0007：实时语音对话架构与可替换引擎层

- 状态：**草稿**（jason 2026-09-08 口述愿景，待选型验证补完后定稿）
  - 🔄 **2026-09-14 回填**：**§4（引擎表 / §4.3.0 / §4.4 / §4.5 / §4.6 / §4.7）与 §6
    已按本轮的实际实现改过**，改动处都带「2026-09-14」标记。
    **§1 / §2 / §3 / §5 / §7 基本未动**——那些还停在「调研与规划」的状态，
    与实现进度无关。本文整体**仍是草稿**：V2 的候选没关掉，TTS 没做人耳验收，
    三条打断出口判据没测。
  - ⚠️ **回填只改「已经发生的事实」，不改「当初的判断」**：被实现推翻的措辞
    （如 §3 表里「VAD 检测到开口就掐掉 TTS」）**保留原文并就地标注**，
    不静默改写历史。
- 取代/修订：修订 `CLAUDE.md`「双工的正确理解」一节；修订 ADR-0001 §排除 Qwen3-ASR 的理由
- 相关：ADR-0002（理解层 / 边车纪律）、ADR-0004（泰语引擎）、ADR-0005（TTS 选型，草稿）

---

## 1. 背景：愿景的一次重述

jason 2026-09-08 口述，把产品形态说清楚了一层。原话要点：

> 类似于一个实时双工的 AI。我们做的主要是语音部分。这个 App 有个按钮，
> 类似于打电话，打通了之后，双工可打断地实时跟我交流沟通。
> 我的语音你能识别并转化为内容，然后交给大模型 —— **当然这个大模型并不在
> 当前项目的工作职责范围内**，但我可以做一个 mock 的模型，简单回复一下。
> 比如我问今天天气怎么样，你去调用一下天气接口，告诉我「今天清迈天气还不错」。
> 用文字和语音**同时**输出，或者只用语音也可以。
> 语音是中文、英文和泰语，**可配置可选**，沟通前确认用什么语言，
> **过程中可以随时切换**，包括我说的和你输出的。
> 音色模型是可选的，并且要**真正像人类一样有感情**地说话。
> 所有模型必须本地化。

这段话与既有文档有五处实质冲突，逐条处理见 §2。

### 1.1 交付形态（本次新增的约束）

产品最终要能以三种方式被使用，**这决定了架构必须分层**：

1. **装到电脑上直接用** —— 现有 `.app` 形态，加一个「通话」入口
2. **配置自己的后台模型** —— 语音进、语音出，中间那个 LLM 由用户配置接上
3. **代码级集成到别的 agent / 模型** —— 要预留接口和说明文档

> **原有的录音输入法形态继续保留**，不被通话形态取代。

---

## 2. 与既有文档的冲突处理

| # | 愿景 | 既有文档 | 处理 |
|---|---|---|---|
| 1 | 实时全双工可打断 | `CLAUDE.md`：「2 GB 预算内做不到，v1 是打断式半双工」 | **分级**，见 §3 |
| 2 | 中英泰可配置、可随时切 | ASR 双链路（SenseVoice 无泰语 + whisper 泰语） | **统一到单引擎**，见 §5 |
| 3 | 有感情、音色可选 | ADR-0005 只验证了「能出声」 | 见 §6 |
| 4 | 打电话式实时对话 | M1 是快捷键 toggle 单次录音 | **新增会话层**，见 §4 |
| 5 | mock LLM（查天气） | 「LLM 不在职责内」 | LLM 仍不在职责内，但**要自带一个可跑通的默认实现**，见 §4.3 |
| 6 | 全本地 | 已是不可让步约束 | 一致，无冲突 |

### 2.1 关于「全双工」的措辞修订

`CLAUDE.md` 原文断言全双工「在 2 GB 预算内做不到」。**这个论断的前提已经不成立**
（M2 起预算 ≤9 GiB，目标机 64 GB），但结论方向仍然成立，原因变了：

**新的理由不是内存，是没有「已验证覆盖中泰且达到实时」的本地全双工候选**
——注意是「没有已验证的」，不是「不存在」。调研（2026-09-08）：

- NVIDIA VoiceChat 11B —— 真全双工、~450 ms 转轮延迟，但**仅英文**、
  许可证 OpenMDW 1.1 **仅研究用途**、aarch64 上 RTF 1.13 **达不到实时**
- PersonaPlex 7B（`speech respond`）—— 全双工，**语言覆盖与性能都还没测**

所以**不要再用「内存不够」解释为什么不做全双工**，那会误导后续决策。
**同时：PersonaPlex 那格没关掉之前，也不要写「不存在方案」。**

---

## 3. V1 / V2 分级：把内存门槛做成产品档位

jason 的原话：

> 不是所有人都有 64G 内存，所以可以有个降级的选择。内存不够的，
> 可以打断式的半双工。V2 就是实时全双工。

**这是产品决策，不是妥协**——它让 AgentEar 在低配机器上仍然可用。

| 档 | 形态 | 内存门槛 | 状态 |
|---|---|---|---|
| **V1** | 打断式半双工（VAD 检测到开口就掐掉 TTS） | **≤10 GB** | M3 目标；🔄 **2026-09-14：链路已跑通，「VAD 自动打断」这一半还没做**，见下 |
| **V2** | 实时全双工（边听边想） | 放宽到 **64 GB** 级 | **未排期**，见下 |

> 🔄 **2026-09-14 就地表态**：上表「VAD 检测到开口就掐掉 TTS」是**目标形态，不是现状**。
> 本轮落地的打断入口是**推键式**——用户按录音键时先掐掉正在播的回答再开麦克风
> （`src/main.rs` 的录音键路径 → `talk::stop_playback()`）。
> **没有 VAD 自动打断，也没有双讲**；因此「误打断率」与「端到端打断延迟 <300 ms」
> **仍未测**。详见 §4.3.0 末段与 §8。

**V2 不占里程碑编号。** 它的**触发条件**是「出现经验证覆盖中文和泰语、
且在 Apple Silicon 上达到实时的本地全双工模型」——这个条件今天不成立，
排期也就无从谈起。条件满足时再立编号。
（初稿曾写「M4」，那个编号已经被「录音笔接入」占用，是笔误。）

**V1 不是 V2 的临时替代品，是长期保留的低配档位。** 两档共用同一套会话层
和引擎层，差别只在编排策略。

⚠️ **V1 的措辞纪律不变**（沿用 `CLAUDE.md`）：半双工解决「说完才轮到我」，
不解决「一边听一边想」，快速来回时能被感知到。**不要描述成「体感接近全双工」。**

---

## 4. 架构：R0–R3 四层，向下可换引擎，向上可换外壳

> ⚠️ **命名注意**：本仓库的 `L0–L3` 已经有确定含义
> （事实层 / 文档层 / 索引层 / 行动层，见 `CLAUDE.md` 存储语义一节）。
> **运行时架构一律用 `R0–R3`**，不要复用 L 前缀，否则同一个词两套含义。

```
┌────────────────────────────────────────────────────────────┐
│ R3 外壳层（三种，共享同一内核）                              │
│  A. AgentEar.app        桌面常驻：录音输入法 + 通话入口       │
│  B. agentear serve      HTTP / WebSocket，OpenAI 兼容        │
│  C. agentear mcp        MCP server，接入别的 agent 流程       │
└───────────────────────────┬────────────────────────────────┘
                            │
┌───────────────────────────▼────────────────────────────────┐
│ R2 agentear-core（Rust lib）—— 可被代码级集成                │
│  会话生命周期 · 语言切换 · 持久化策略 · 既有 route/kb/index    │
│  ⚠️ 编排职责归属见 §4.4，不能和后端各编排一次                  │
└───────────────────────────┬────────────────────────────────┘
                            │
┌───────────────────────────▼────────────────────────────────┐
│ R1 实时音频图（**同进程**，见 §4.3）                          │
│  音频 I/O · AEC · VAD · 轮次判定 · 打断控制                    │
└───────────────────────────┬────────────────────────────────┘
                            │ 进程边界只画在这里
┌───────────────────────────▼────────────────────────────────┐
│ R0 模型推理后端（子进程 / HTTP 边车，实现语言不受限）          │
│  AsrEngine │ sensevoice(现) │ whisper-th(现) │ qwen3-asr      │
│  TtsEngine │ voxcpm2(现)    │ say(现)        │ indextts2      │
│  LlmEngine │ MiniCPM5-2B(现)│ mock(现)       │ 任意 OpenAI 端点 │
└────────────────────────────────────────────────────────────┘
```

🔄 **2026-09-14 引擎表按实际实现更新**（左格是接口，右格是当下真实存在的实现）：

| 接口 | 实现 | 状态 | 备注 |
|---|---|---|---|
| `AsrEngine` | `builtin`（SenseVoice + whisper 泰语）/ `speech_swift` | 既有 | 本次通话链路**仍走既有 ASR**，未换 Qwen3-ASR（见 §5）。泰语时延数据在本环境无效，见 `progress.md` |
| `TtsEngine` | **`http` → `services/tts`（默认 VoxCPM2-4bit，48 kHz）** | ✅ 2026-09-14 | `src/talk.rs::HttpTts`；HTTP 契约 `POST /speak {text,lang}` → `audio/wav` |
| `TtsEngine` | **`say`（macOS 内置，零依赖）** | ✅ 2026-09-14 | `src/talk.rs::SayTts`，**降级为保底** |
| `TtsEngine` | `indextts2` | ❌ 未接 | 情感控制更强但无泰语，见 §6 |
| `LlmEngine` | **`openai_compat`（默认指向 `127.0.0.1:8794` 的 MiniCPM5-2B-4bit 边车）** | ✅ 2026-09-14 | 任何 OpenAI 兼容端点都行，**换模型只改 URL**；`strip_thinking` 兜掉 ` thinking` 段 |
| `LlmEngine` | **`mock`（`WeatherMock`）** | ✅ 2026-09-14 | **零依赖、写死回答、必须显式配置才启用**，见 §4.6 |
| `AecEngine` | 无（VPIO 是系统能力，不是引擎实现） | — | 仍未抽象，见 §4.2 / §4.3.0 |

### 4.1 这一层不是新发明的

`src/asr.rs:118` 的 `transcribe(wav, lang)` **已经在按语言分派**到
`transcribe_sensevoice` 和 `transcribe_thai` 两个子进程后端。R0 是把这个
既成事实提升为显式 trait，**不是推倒重来**。

### 4.2 设计原则（含一条被推翻的）

1. **LLM 永远是「可配置的外部端点」。** 我们不训练、不绑定、不内嵌 LLM。
   自带的 mock 只用来证明链路通，不是产品能力。
2. **能力矩阵，而不是「每个 trait 至少两个实现」。**
   ~~初稿写了「每个 trait 至少两个实现 + 每个都有零依赖默认实现」，这条是错的~~：
   `AecEngine` 目前只有 LocalVQE 一个候选，ASR / AEC 也不存在能满足产品语义的
   零依赖实现。硬套这条规则只会逼出没意义的 no-op 适配器。**改成：**

   | 引擎 | 多后端 | 零依赖默认实现 |
   |---|---|---|
   | AsrEngine / TtsEngine / LlmEngine | ✅ 必须支持切换 | TTS 有（`say`）、LLM 有（mock）、**ASR 没有** |
   | VadEngine / TurnCompletionEngine | 按会话模式声明 required/optional | — |
   | AecEngine | ❌ 当前仅一个候选，**先不抽象** | 无 |

   只有确实出现第二个候选、且有切换需求时才抽象接口。

   🔄 **2026-09-14 按这张表落地的情况**：`LlmEngine` 与 `TtsEngine` 各有**两个**实现
   （LLM：`openai_compat` / `mock`；TTS：`http` / `say`），切换全靠配置、不重编译；
   `AecEngine` **仍然没有抽象**（VPIO 是系统能力，不是可替换引擎），
   `VadEngine` / `TurnCompletionEngine` 也还没抽象——本轮没有 VAD 自动打断的需求，
   硬抽象会逼出没意义的 no-op 适配器。

3. **进程边界只画在 R0/R1 之间**（见 §4.3），不是「每个 trait 都跨进程」。
   🔄 **2026-09-14**：本轮跨进程的只有 LLM 与 TTS 两个 HTTP 边车
   （`:8794` / `:8765`），**都在 R0 侧**；音频播放（`afplay` 子进程）与会话状态机
   都在守护进程内。

### 4.3 为什么 AEC / VAD 不跨进程

初稿把五个引擎一律放到进程外，**这在实时音频上行不通**：

- **AEC 必须同时拿到麦克风流和扬声器参考流**，两路要严格对齐时钟。
  跨进程传两路 PCM 还要保持采样级同步，代价直接体现在打断延迟上。
- 打断响应的验收阈值是 **< 300 ms**（`asr-selection.md` §4）。
  每加一次跨进程往返就吃掉一部分预算。

**所以：音频 I/O、AEC、VAD、轮次判定、打断控制共置在同一个实时音频图（R1）里；
只有模型推理后端（R0）跨进程。** 若将来确实要把 AEC 挪出去，
ADR 必须先给出帧协议、时钟对齐方案和延迟预算，并用实测证明达标。

#### 4.3.0 AEC 用 CoreAudio VPIO，不用 speech-swift（T3.4.0 实测定案）

`speech` CLI 与 `speech-server` **都没有 AEC 出口**（实测相关端点全部 404；
只有 `denoise`/`restore` 两个降噪命令，不是 AEC）。LocalVQE v1.4-AEC
**只存在于 Swift 库**（`import SpeechEnhancement`）。

→ 采用 **macOS Voice Processing I/O**
（`AVAudioEngine.inputNode.setVoiceProcessingEnabled(true)`）：
它是系统能力、不引入任何依赖，且天然与音频 I/O 共置，正合 §4.3 的要求。

**实测（benchmarks-m3.md §7.2，5 轮有效重测）**：VPIO 关时 **5/5** 完整复现 TTS 内容；
VPIO 开时 **5/5 都不再复现**（内容级 0/5 PASS），**但事件级仍 5/5——每轮残留一个单字**
（`嗯`/`金`/`啊`），**自触发率未到 0**。
⚠️ 早前写的「4/4」出自**第一批因扬声器静音而作废的数据**，已按有效重测更正。
补救靠最小语音时长 + 能量门限 + 播放期 gating，属 T3.4.2。
⚠️ VPIO 的抑制量**方差很大**（一轮里出现过开启后反而更高），不要用单次测量下结论。

#### 🔄 4.3.0a 2026-09-14：本轮的解法是「推键式」，**AEC 那一格没有被解决**

**先把结论说清楚：AEC 仍然没解决，只是被绕开了。**

- **落地的是什么**：用户按录音键时，守护进程**先 `talk::stop_playback()` 掐掉
  正在播的回答，再开麦克风**（`src/main.rs` 的录音键路径；顺序反了就会先录到
  自己正在放的声音）。这是**推键式（push-to-talk 式）打断**——
  **用户主动按键**才有打断，程序不会自己发现「他开口了」。
- **没有做的是什么**：**VAD 自动打断没做**，**双讲（双工）没做**，
  §4.3.0 里提的**最小语音时长 / 能量门限 / 播放期 gating 一条都没实现**。
  所以「播放时麦克风收到的残余被 ASR 解释成单字」这件事**依旧会发生**。
- **因此三条出口判据全部仍未验收**：**自触发率 = 0**（T3.4.0 实测事件级 5/5）、
  **端到端打断延迟 < 300 ms**、**误打断 < 1 次 / 10 分钟** —— 后两条**本轮一次都没测**。
- ⚠️ **写文档时的措辞纪律**：可以说「V1 的通话链路已跑通」，
  **不能说「打断已解决」**，更不能说「体感接近全双工」。
  推键式解决的是「用户想插话时能立刻插话」，**不是**「系统能自动让出轮次」。

### 4.3.1 VAD 与轮次判定是两件事，不是两个实现

~~初稿把 Silero 和 Smart Turn 并列成 `VadEngine` 的两个可选实现，这是错的。~~
它们是**串联关系**：

```
Silero VAD 检出静音  →  TurnCompletionEngine（Smart Turn）判断这个停顿
                          是不是「说完了」  →  最大静默时长兜底
```

Silero 回答「现在有没有人在说话」，Smart Turn 回答「这个停顿意味着轮次结束吗」——
后者正是用来**避免句中换气被误判成说完**。二选一会丢掉其中一个能力。

### 4.4 编排职责必须二选一，不能两边都做

`speech-server` 的 `/v1/realtime` **自带**轮次检测（`server_vad`）、
commit 和取消语义。如果 R2 也跑一套会话状态机，就会出现**两个状态机抢同一件事**。

**二选一，在 M3.0 定死：**

| 方案 | R2 的角色 | R0 的角色 |
|---|---|---|
| **A 自主编排** | 独占会话状态机与打断编排 | 只提供原子的 ASR / TTS 调用，**不用 `/v1/realtime`** |
| **B 委托编排** | 只管生命周期与持久化 | `RealtimeSessionBackend`，整段会话交给 `/v1/realtime` |

**倾向 A**：打断语义和持久化耦合紧，且 A 不把会话行为绑死在 v0.0.x 的上游协议上。
但这是 M3.0 要正式拍板的事，**不能默认它已经定了**。

#### ✅ 2026-09-14：**已拍板并落地 —— 选 A（R2 自主编排）**

- **R2 的角色**：`src/session.rs` 独占会话状态机与打断编排，相为
  `Idle / Listening / Thinking / Speaking / Failed`；对外接口是
  `begin_turn` / `finish_listening` / `turn_ready` / `speaking_done` / `barge_in` /
  `set_lang` / `fail` / `hang_up` / `turn_elapsed`。
- **R0 的角色**：只提供**原子的** ASR / LLM / TTS 调用
  （`src/talk.rs` 的 `LlmEngine` / `TtsEngine`），**没有用上游的 `/v1/realtime`**。
- **代价（写了就要认）**：轮次语义、打断语义、语言切换的合法时机**全部由我们维护**，
  拿不到上游 `server_vad` / commit / cancel 的现成实现；
  换 ASR 或换 TTS 后端时，会话层不需要跟着改（这是选 A 换来的）。
- **语言切换的落地细节**：`set_lang` 在通话中任意时刻生效，
  但**在 `Thinking` 相拒绝**——上一轮的提示词已经发出去了，此刻改语言
  只会让回答的语言与用户刚说的话错位。理由写在 `src/session.rs` 的文档注释里。
- **两条边界案例已钉成测试**：**空转写不记轮次**；
  **LLM 失败时转写照样记一轮**（`reply: None`）——用户说了话这件事不能被下游失败抹掉。
- ⚠️ **这不代表 §4.5 的持久化策略也跟着做了**，见下。

### 4.5 持久化：路径 A 零丢失、路径 B 有界丢失——**V1 通话属于路径 A**

~~初稿在会话内核里笼统写了「零丢失提交协议」，这与 `CLAUDE.md` 冲突。~~
既有措辞纪律（`CLAUDE.md` 存储语义一节 / `ingest-design.md` §3.7）是：

- **文件导入（路径 A）= 零丢失**，走完整提交协议后才 ACK
- **实时流（路径 B）= 有界丢失**，崩溃会丢掉最后一次 fsync 之后的部分

这两条策略本身仍然成立、不要删掉：

| 策略 | 用于 | 保证 |
|---|---|---|
| `BatchCommitPolicy` | 文件导入、M1 的快捷键录音、**V1 通话（推键式）** | 零丢失 |
| `StreamCheckpointPolicy` | 持续开着、没有明确终点的实时流（路径 B 场景） | 定长时间片切分 + fsync 窗口内有界丢失 |

**下游路由仍然只消费 committed 的转写，不消费 provisional 的。**

✅ **2026-09-19 定案（推翻本节初稿的判断）：V1 通话不属于路径 B，属于路径 A，
`StreamCheckpointPolicy` 不需要实现。**

本节最初假设"通话形态属于路径 B"，前提是设想中的"持续开着、VAD 自动检测
轮次"的电话式通话——那种场景确实没有明确终点，只能接受有界丢失。但
**V1 的打断方式已经拍板为推键式长期终态**（§8 第 5 条），推键式下每次
录音天生就是"按下开始、松开结束"的有头有尾对象，跟 M1 的快捷键录音是
**同一套机制、同一条路径 A**。

- **现在这条链路走的是 `BatchCommitPolicy`（零丢失），这不是"还没升级到
  位"，是已经用对了**——零丢失比有界丢失更好，不是退而求其次。
- `StreamCheckpointPolicy` 这一行**留在表里，供将来真正的路径 B 场景用**
  （比如 V2 全双工、或者哪天真的要做持续开着的 VAD 自动通话）——
  **不是删掉这个策略，是确认 V1 用不上它**。

### 4.6 mock LLM 的范围

一个「查天气 → 用语音说出来」的最小实现，用来：
- 证明 ASR → LLM → TTS 整条链路通
- 给集成方一个可照抄的 `LlmEngine` 实现样例

**它不是产品功能**，文档要写明这一点，避免被当成智能助手来评价。

🔄 **2026-09-14：已实现（`src/talk.rs::WeatherMock`），并且做到了下面三条**：

1. **必须显式配置才启用**（`talk_llm_engine: "mock"`）。**默认成 mock 会让人以为
   模型在跑**——默认是 `openai_compat`，指本机 `:8794` 的 MiniCPM5-2B 边车。
2. **「本地天气事实」是写死的场景，不是天气接口。**
   配置项只有 `talk_city`（默认「清迈」）与 `talk_weather_note`；
   默认那句是「今天{城市}多云转晴，最高 32 度，傍晚有阵雨，风不大。」。
   **接真实天气源是集成方的事**（R3 外壳层），**不在本项目职责内**。
   `scripts/talk-e2e.sh` 与 ADR-0005 的更新块都重复了这条，不许省。
3. **三语端到端实跑过**（中 / 英 / 泰，`scripts/talk-e2e.sh`），
   输出同时有**文字**（听到什么、答什么）与**语音文件**。
   ⚠️ 但**泰语在 LLM 能力边界外**（MiniCPM5-2B 模型卡只声明 en/zh）：
   实测能听懂泰语问句、也能产出泰语，**提示词不钉死语言时会用中文回答**。
   **不要把泰语质量写成与中英同级。**

### 4.7 输出模态

文字与语音**同时输出**为默认，可配置为「只语音」或「只文字」。
文字那一路直接复用现有的剪贴板 / 悬浮窗通路。

🔄 **2026-09-14：只落地了一半。** 现状是——
- **用户说的话**：照旧进剪贴板 / 上屏（既有链路一行没改）；
- **模型的回答**：`println!` 到 stdout、并写 `~/.agentear/agentear.log`，**同时**用语音播出；
- ❌ **回答没有进剪贴板 / 悬浮窗**；
- ❌ **「只语音 / 只文字」的可配置开关没做**（现在固定是「文字打日志 + 语音播放」）。

---

## 5. ASR：候选是统一到单引擎

> 实测数据见 [`docs/benchmarks-m3.md`](../benchmarks-m3.md)（2026-09-08）。

Qwen3-ASR 一个模型覆盖 30 语言（含中/英/泰）+ 22 中文方言，**自带语言识别**——
这直接实现「过程中随时切语言」而不需要用户手动切菜单。

泰语实测（22 条 Arm 语料，同一份 `reference.tsv`、同一套 CER 口径；
**但两侧的提示词表来源不同**，见 benchmarks-m3.md §2.1.0）：

| 配置 | 纯泰语 CER | 夹英文 CER | 英文词命中 |
|---|---|---|---|
| whisper A（v0.4.2 当前发布） | 3.9% | 31.1% | 8% |
| whisper D（词表 prompt） | 3.1% | 18.4% | 51% |
| Qwen3-ASR 1.7B + 24 词 canonical | **0.0%**（6/6 全对） | 23.1% | 44% |
| **Qwen3-ASR 1.7B + 47 词（与 D 同源词表）** | **0.0%** | 23.1% | 44% |

**换成与 whisper D 同源的完整 47 词拉丁词表，22 条结果逐字不变**，
所以词表口径这个变量已排除。

**本样本上纯泰语更好**（6 条、单一说话人，不足以断言总体）；
**夹英文两个指标都方向性偏向 whisper D**，但抽样不确定性未量化——
配对检验做不了，因为 whisper 的逐条 CER 没入库（benchmarks-m3.md §4.1）。
3 轮复跑 22/22 完全一致，解码是确定性的。
离线 RTF 0.074，**但实时指标（首包延迟、打断响应 <300ms、AEC 自触发率）一项都没测**。

⚠️ **这会推翻 ADR-0001 排除 Qwen3-ASR 的理由**（原因是「需要 Python + MLX 运行时」）。
speech-swift 是 **Swift 原生 + CLI/HTTP**，那条理由不再适用。
资源那一关已由 **2026-09-08 的分档拍板**解决（`CLAUDE.md`：
默认档 ≤2 GiB / 高资源档 ≤4 GiB，`speech_swift` 的 2.43 GiB 落在高资源档）。
**但「换默认引擎」是另一件事，仍需单独拍板**——那要看中英横比（T3.5.1）。

⚠️ **中文和英文链路还没横比。** 换默认引擎影响全部语种，
不能凭泰语结果推断 SenseVoice 该不该换。**拍板前这是个缺口。**

🔄 **2026-09-14 补一句实况**：本轮通话链路**仍然用既有 ASR**
（`builtin`：SenseVoice 中英 + whisper 泰语；`scripts/talk-e2e.sh` 里 `--lang` 只对泰语加），
**没有切到 Qwen3-ASR / `speech_swift`**。所以 §5 的选型结论**一点没被本轮推动**，
`T3.5.1`（中英横比）仍是拍板前置。

⚠️ 另外：**本 sandbox 里测出的 whisper 时延数字一律不可引用**（Metal 着色器缓存写不进去，
实测 19.9s vs 记录 0.96s，CPU-only 5.9s）——不是模型变慢。详见 `docs/agent/progress.md`。

---

## 6. TTS：泰语和情感可能要分两个引擎

> 实测数据见 [`docs/benchmarks-m3.md`](../benchmarks-m3.md) §6。

筛选条件按优先级：**泰语 > 可商用 > 情感控制 > Apple Silicon**。

- **VoxCPM2**（Apache-2.0，48 kHz）：**是唯一能说泰语的候选**。
  `--voxcpm2-instruct` 用自然语言描述情感，实测客观有效
  （兴奋档 RMS 是中性档 6 倍、悲伤档时长更长）。
- **IndexTTS2**：情感控制强得多（`eager/happy/excited/angry/sad/afraid`
  预设 + 8 值向量 + 强度系数），**但只有 EN/ZH，没有泰语**。

→ 很可能的落点是**按语言选 TTS 引擎**：中英 IndexTTS2、泰语 VoxCPM2。
这正是 §4 引擎适配层要支撑的场景。

🔄 **2026-09-14：落点已定，且比上面这句更简单——暂时不需要按语言分引擎。**

| 项 | 实际落地 |
|---|---|
| 引擎 | **`mlx-community/VoxCPM2-4bit`**，经 `mlx-audio`，48 kHz |
| 载体 | `services/tts` 的 HTTP 契约不变：`POST /speak {text,lang}` → `audio/wav`，`--backend voxcpm2`（**默认**） |
| 保底 | `--backend say`（macOS 内置三语音色），**零依赖、零下载**，已降级为兜底 |
| Rust 侧 | `src/talk.rs::HttpTts`（`tts_url`，默认 `127.0.0.1:8765`）/ `SayTts`；**默认关**（`talk_enabled`） |
| IndexTTS2 | ❌ **没接**。中英与泰语现在共用 VoxCPM2——分引擎只有在中英质量对比做出来之后才谈得上 |

**实测（2026-09-14，M1 Max / 64 GB）**：48 kHz 单声道 16bit；模型加载 **0.86s**；
HTTP 端到端一句 **2.6–3.9s**（一次完整轮次里合成 2.6–5.1s）；**进程峰值 RSS 约 2.5 GB**。
三语端到端都跑通；**回环验证语言正确**——zh 合成音频送 SenseVoice 得回
「今天青迈天气还不错，最高32度。」，泰语送 whisper(th) 得回
「วันอีกที่เชียงใหม่อากาศดีอุณหภูมิ 32 องศา」，**即说的确实是目标语言**。

⚠️ **这几条不要外推**：
1. **4bit 与 bf16 的质量对比没做。** 只测了「能跑、多快、多大」。
   本节开头引的 `benchmarks-m3.md` §6 那批 VoxCPM2 数据走的是
   **speech-swift 的 bf16 路径**，与本次的 **MLX 4bit** **不是同一个运行时，别混着引用**。
   本轮的明细在 [`docs/benchmarks-talk.md`](../benchmarks-talk.md)——
   两份数字不能互相引用，**连「4bit 更省内存」都不成立**（2.5 GB 是 Python + MLX
   进程峰值，1.54 GiB 是 speech-swift 进程峰值，口径不同）。
2. **「情感」一个字都没评。** 本节开头「RMS 是中性档 6 倍」那种指标
   **区分不了「真有情绪」和「单纯调大音量」**，仍要人耳。
3. **方言触发仍无客观判据**（见 §6.1）。
4. **泰语语音本身没问题**（回环转写逐字正确），但**TTS 说的是泰语 ≠ LLM 会好好说泰语**——
   LLM 侧的泰语边界见 §4.6。

### 6.1 方言清单：一个新的硬约束（jason 2026-09-08 追加）

原方言清单（台湾话/广东话/香港话/英语/泰语）**追加了山东话、河南话**。
对照 VoxCPM2 官方支持的 9 种中文方言（四川话、粤语、吴语、东北话、河南话、
陕西话、山东话、天津话、闽南话）：

| 要求 | VoxCPM2 | 备注 |
|---|---|---|
| 普通话 / 粤语（广州话）/ 山东话 / 河南话 | ✅ | 触发方式待验证 |
| 香港腔 | ⚠️ | 用粤语近似 |
| **台湾腔** | ❌ | **闽南话 ≠ 台湾国语腔**，不要混为一谈 |

**台湾腔已降级（jason 2026-09-08 拍板）**：不再是选型的筛选条件。

这条影响很大——它把 **FireRedTTS3 从候选里拿掉了**。原本引入它的唯一理由
就是台湾腔（21 种中文方言），而代价是多一个运行时 + 声音克隆限学术使用。
台湾腔降级之后，**VoxCPM2 的 9 种方言足够覆盖当前需求**，不需要第二个 TTS 引擎。

香港腔仍用粤语近似。台湾腔记为**已知缺口**，将来真有需求再单独立项。

⚠️ **方言是否真的能靠 instruct 触发，目前没有客观判据**
（`language-id` 只到语言级，分辨不了中文内部方言），见 benchmarks-m3.md §6.2.2。

### 6.2 人耳验收状态

| 项 | 状态 |
|---|---|
| 泰语语种正确 | ✅ `language-id` 判定 Thai 99.3% |
| 泰语听感 | ✅ **jason 2026-09-08：「还可以」** |
| 中文音色 | ⚠️ 第一批太尖锐（instruct 用了 "energetic young woman"，是配置问题不是模型问题），已重做 4 个音色待听 |
| 中文方言 | ❓ 待听 |
| 🔄 三语链路出声（2026-09-14，MLX 4bit） | ✅ **能出声、说的是目标语言**（回环转写逐字正确）——**这条不是听感验收**，只是自动化的语言正确性检查 |
| 🔄 音色 / 情感 / 方言（2026-09-14） | ❓ **一个字都没评**，仍等 jason 人耳（`progress.md` 的 Q3） |

⚠️ **「像真人有感情」这个要求，声学指标验证不了。**
RMS 和过零率区分不了「真有情绪」和「单纯调大音量」，也判断不了泰语发音对不对。
**必须人耳验收**，样本已生成在 `~/Desktop/agentear-tts-samples/voxcpm2-2026-09-08/`。
**在听完之前不要把任何 TTS 写成已选定。**

🔄 **2026-09-14 的边界（上面那句仍然有效，但要拆成两件事）**：
- **「实现落点」定了** —— VoxCPM2-4bit（MLX）接进 `services/tts`，`say` 降为保底，
  Rust 侧已接（默认关）。可以说「TTS 走 VoxCPM2-4bit」。
- **「语音质量达标」没定** —— 音色、情感、方言**一个字都没评**（人耳）。
  **不可以写「VoxCPM2 已选定为达标的 TTS」**，更不可以写「情感表现好」。

---

## 7. 对已发布版本的影响

**结论：不影响，新引擎以并存方式加入。**

v0.4.2 已发布。既有用户的链路（SenseVoice + whisper 泰语）**保持默认不变**，
speech-swift 后端作为**可选项**通过配置启用。理由：

- 11572 行代码里，受影响的只有 `asr.rs` 的引擎分派和 `download.rs` 的模型常量
- `store` / `route` / `kb` / `index` / `label` / `terms` / `correct` / `deliver`
  这些「先存后分流」的骨架**一行不用动**
- 换引擎是**配置项**，不是版本升级的强制动作

**只有在实测数据支持、且 jason 拍板之后，才改默认值。**

---

## 8. 待定项

1. Qwen3-ASR 是否取代双链路成为默认 —— 需 jason 拍板（见 §5）
2. TTS 泰语情感是否达标 —— 需人耳验收
3. V2 全双工的模型 —— **目前没有「已验证覆盖中泰且达到实时」的候选**，保持观望。
   ⚠️ 措辞按 §2.1：**不是「不存在候选」**——PersonaPlex 的语言覆盖只是没测，
   那一格没关掉之前不许写成不存在。
4. 集成接口（`serve` / `mcp`）的具体协议 —— M3.4 再定

### 🔄 2026-09-14 新增/变动的待定项

5. ✅ **已拍板（jason 2026-09-19）：V1 的打断入口就定为「推键式」，长期形态，
   不补 VAD 自动打断 / 双讲。** 用户按录音键才打断，不支持「开口说话系统
   自动让出轮次」。**这不是妥协、是 V1 的完整定义**——真正的全双工留给
   V2（§3，触发条件是出现已验证覆盖中泰且达到实时的本地全双工模型，
   目前不成立）。
   - **连带结论**：VAD 自动打断需要的前置工作**不再是 V1 的必做项**，
     见下面第 6 条。
   - **两条出口判据的适用范围收窄**：「误打断率 <1 次/10 分钟」是
     VAD 自动检测才会出现的现象（推键式没有"系统误判你要打断"这回事，
     打断由用户主动按键触发），**对 V1 不适用，不用测**。「端到端打断
     延迟 <300 ms」在推键式下含义变了——不是"系统多快反应过来"，是"按键
     到声音真的停"，这个链路很短（杀掉播放子进程），**大概率已经达标，
     但要实测一次才能确认，不能假设**。
6. **AEC 事件级自触发（5/5 残留单字）—— 按第 5 条的拍板，不再是 V1 的
   必做项。** 推键式打断不依赖"系统听出你在说话"，AEC 残留单字这个问题
   对推键式没有影响。**保留这一条作为已知限制记录**，但不再是 T3.4.2
   收口的前置——除非将来真的要重开 VAD 自动打断这条线（那时候它又会
   变成前置）。
7. ✅ **已拍板（jason 2026-09-19）：`StreamCheckpointPolicy` 不需要了，是第 5
   条拍板的直接推论。** `StreamCheckpointPolicy`（有界丢失）当初是为"VAD
   自动检测、持续开着的通话流"设计的——那种场景没有明确的录音终点，不能
   等"说完"才落盘。但推键式下每次录音天生就是"按下开始、松开结束"的
   有头有尾对象，跟 M1 的录音输入法是同一套机制，**根本不存在
   `StreamCheckpointPolicy` 要解决的那种"没有明确终点、崩了只丢最后几秒"
   的场景**。通话继续走 `BatchCommitPolicy`（零丢失）**不是退而求其次，
   是已经用对了**——零丢失比有界丢失更好。§4.5 那张表在推键式是 V1 长期
   终态的前提下不再需要新增一行，`BatchCommitPolicy` 覆盖了 V1 的全部场景。
8. **通话入口的形态** —— 起/停一次通话是走菜单栏、快捷键还是独立窗口，
   **仍未定**。第 5 条只拍了"打断方式"，没有涉及这一条，不要混为一谈。
9. **回答的落地形态（§4.7）** —— 回答要不要进剪贴板 / 悬浮窗，
   「只语音 / 只文字」开关要不要做，未定。
