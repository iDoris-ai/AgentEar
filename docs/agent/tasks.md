# AgentEar 任务台账 — Task

> 前置：[`roadmap.md`](roadmap.md)（M→F）·[`architecture.md`](architecture.md)（边界）·[`spec.md`](spec.md)（数据模型）
> 每个 Task 自包含，可独立开发与验收。**验收标准必须可机器验证**。
> 状态：BACKLOG · READY · IN_PROGRESS · BLOCKED · PR_OPEN · CHANGES_REQUESTED · APPROVED · DONE

**本仓库的强制规矩（每个 task 都适用）**：
- 不许直推 main。单主干仓库，PR 直接开向 main，合并要带 `--allow-trunk`。
- 提 PR 前跑**一轮** codex 自我挑战并修完。**一轮即可，不要连刷多轮**——
  jason 2026-09-02 明确要求过。
- 改了 `scripts/*.sh` 必须跑 `scripts/lint-shell.sh`（bash 3.2 的全角字符坑）。
- LLM 边车要在跑（`scripts/serve-llm.sh`），否则 F2.1/F2.2 的验收命令必失败。

---

## F2.1 — 项目术语表

### T2.1.1 术语表数据结构与默认表  `DONE`
- **优先级**：high
- **目标**：让纠错时有一份本项目固定词汇的清单可用，而不是每次从上下文猜。
- **开发范围**：按 spec.md §1 实现 `~/.agentear/terms.json` 的读写：默认表内置、
  首次启动写入、已存在不覆盖、解析失败退回默认表并记日志。把术语表拼进
  `correct.rs` 的提示词。
- **明确不做**：不做逐字符替换（会误伤用户真的在说 road 的情况）；不做菜单编辑界面。
- **依赖**：无
- **交付物**：`src/terms.rs`；`correct.rs` 注入术语表；默认表覆盖 M0 已知的全部错例。
- **验收命令**：`cargo test terms` 全绿，且包含「文件损坏退回默认表」「已存在不覆盖」两条用例
- **涉及文件**：`src/terms.rs`、`src/correct.rs`、`src/main.rs`
- **风险/回滚**：术语表进提示词会增加 token 数、拉长纠错耗时。若实测超过 15 秒需在 PR 里说明并给出取舍。
- **证据**：PR #11（合并 commit af1f695）。提示词格式踩了五版才对，
  详见 PR 描述；`#[ignore]` 集成测试 `real_road_and_id_survive` 是关键——
  没有它前三版都会带着「已修复」的标签合并。

### T2.1.2 长文回归：ro 必须还原成 raw 而不是 repo  `DONE`
- **优先级**：high
- **目标**：把 `docs/benchmarks-m2.md` §8.1 那次真实失败钉成回归测试，防止复发。
- **开发范围**：用 `spike/audio/sample02.wav` 的转写文本做长文用例，断言纠错后
  `raw` 出现且 `repo` 不出现；同时断言其余已知正确的纠正没有退化
  （MacBook / knowledge base / 24 小时 / Mac mini / WiFi）。
- **明确不做**：不重新跑 ASR（用已有转写文本当输入，测的是纠错层不是 ASR）。
- **依赖**：T2.1.1
- **交付物**：`src/correct.rs` 的长文回归测试；`docs/benchmarks-m2.md` §8.1 补上「已修复」的实测结果。
- **验收命令**：`cargo test correct` 全绿；`./target/release/agentear --transcribe spike/audio/sample02.wav` 的输出里出现 `raw 的目录`
- **涉及文件**：`src/correct.rs`、`docs/benchmarks-m2.md`、`tests/fixtures/sample02-asr-raw.txt`
- **证据**：fixture 是一次真实转写的快照（不重跑 ASR，否则 ASR 的随机性会以
  和纠错无关的方式让测试失败）。判据里踩过一个坑：`!contains("repo")` 会被
  `report` 误触发。

### T2.1.4 长文分句后逐句纠错  `DONE`
- **优先级**：high
- **目标**：让术语纠错在长录音上也可靠。
- **背景（2026-09-03 实测）**：术语表在**短句**上稳定有效，
  但 700 字长文连跑 5 次 0 次通过——模型倾向于原样输出。
  T2.1.2 当时报「已修复」是基于单次通过。见 `docs/benchmarks-m2.md` §8.1。
- **开发范围**：按句号/问号/换行分句，逐句送纠错再拼回；
  保持段间空格规则与现有 `join` 一致；给分句加长度上限（过短的合并）。
- **明确不做**：不改术语表内容；不做并发（串行足够，且并发会让边车排队）。
- **依赖**：无（术语表已在）
- **交付物**：分句纠错 + `longform_regression_ro_becomes_raw_not_repo` 转为通过
- **验收命令**：`cargo test --release -- --ignored longform` 连跑 3 次全绿
- **涉及文件**：`src/correct.rs`
- **风险**：逐句调用会让长录音的纠错耗时进一步上升（现在 700 字已经 10 秒）。
  需要实测并在 PR 里给出数字；若不可接受，考虑按段落而非句子切分。

### T2.1.3 术语表可由用户扩展  `DONE`
- **优先级**：mid
- **目标**：jason 能自己加词，不用改代码不用重新编译。
- **开发范围**：菜单加一项「打开术语表」（用 `open` 调系统编辑器）；
  每次纠错时读文件（不缓存到进程生命周期，改完下次录音即生效）；
  文件读失败时退回上一次成功加载的表。i18n 三语文案。
  ⚠️ **codex 已确认「退回上次成功表」目前没实现**（T2.1.1 评审 Medium 5）：
  编辑器截断重写的瞬间读到半文件，会突然退回内置默认表而不是用户的表。
  这个 task 必须真的做掉它，不是顺带。
- **明确不做**：不做图形化编辑器；不做热重载监听（每次读文件足够，几 KB）。
- **依赖**：T2.1.1
- **交付物**：菜单项 + 三语文案 + 读取策略
- **验收命令**：`cargo test i18n` 全绿（新 Key 的三语覆盖由现有测试强制）
- **涉及文件**：`src/tray.rs`、`src/i18n.rs`、`src/terms.rs`

---

## F2.2 — 标签识别与路由

### T2.2.1 重新定义 8 类标签边界并建评测集  `DONE`
- **优先级**：high
- **目标**：把 M0 基准判错的两条从「模型不行」还原成「定义不清」，给出可判别的边界。
- **开发范围**：按 spec.md §2 的判别依据，为 8 个类各写 2 条以上正例；
  把 M0 那两条判错用例（开会讨论 / 帮我查日程）连同判定理由写进评测集；
  扩充 `spike/m2_bench.py` 的标签用例到至少 16 条。**这一步不写产品代码。**
- **明确不做**：不改 `src/`；不做 few-shot 注入（那是 T2.2.2）。
- **依赖**：无（可与 F2.1 并行）
- **交付物**：`docs/agent/label-taxonomy.md`（定义 + 判别依据 + 全部用例）；扩充后的评测脚本
- **验收命令**：`~/.agentear/llm/venv/bin/python spike/m2_bench.py --url http://127.0.0.1:8793` 能跑完并给出扩充后的分数
- **涉及文件**：`docs/agent/label-taxonomy.md`、`spike/m2_bench.py`
- **风险**：Q1（开会讨论该判 note 还是 journal）是产品决策。按 spec.md 先做，**在文档里标为待 jason 确认，不当定论**。

### T2.2.2 标签识别实现  `DONE`
- **优先级**：high
- **目标**：转写之后拿到一个一级标签，失败时降级为 unknown 而不是中断。
- **开发范围**：`src/label.rs`：调边车、few-shot 用 T2.2.1 的用例、只取最后一行非空
  （同 correct.rs 的判据）、解析成封闭枚举、非法值落 unknown。
- **明确不做**：不做二级标签抽取；不做路由落盘（T2.2.4）。
- **依赖**：T2.2.1
- **交付物**：`src/label.rs` + 单元测试（含「模型返回垃圾 → unknown」「边车不可达 → unknown」）
- **验收命令**：`cargo test label` 全绿
- **涉及文件**：`src/label.rs`、`src/main.rs`

### T2.2.3 显式标记优先于模型推断  `DONE`
- **优先级**：high
- **目标**：用户说「这是一个 idea」就必须按 idea 走，模型推断不得覆盖。这是架构边界 B5。
- **开发范围**：中英文显式标记的识别规则（可被单元测试覆盖，**不靠模型判断**）；
  识别到就直接定标签并标记 `label_source=explicit`，跳过模型调用。
- **明确不做**：不做模糊匹配（「我觉得这算个想法吧」不算显式）。
- **依赖**：T2.2.2
- **交付物**：显式标记解析 + 测试（含中英文各 3 种表述、以及「不该被误判为显式」的反例）
- **验收命令**：`cargo test explicit` 全绿
- **涉及文件**：`src/label.rs`

### T2.2.4 routes 落盘  `DONE`
- **优先级**：high
- **目标**：每次转写产出一条 `routes/` 记录，只增不删，可重算。
- **开发范围**：按 spec.md §3 的 JSON 结构落盘到 `routes/yyyy-mm/`；
  写入走「先写临时文件再 rename」（同 store.rs 的既有做法）；
  `delivery.state` 初始为 pending。
- **明确不做**：不做实际投递（ADR-0003 的适配器，不在本轮）；不做重试队列。
- **依赖**：T2.2.2
- **交付物**：`src/store.rs` 的 routes 写入 + 测试（含「标签识别失败仍落盘且 label=unknown」）
- **验收命令**：`cargo test routes` 全绿
- **涉及文件**：`src/store.rs`、`src/main.rs`

### T2.2.5 标签基准回归到至少 7/8  `DONE`
- **优先级**：mid
- **目标**：证明重新定义边界 + few-shot 确实解决了 M0 那 6/8。
- **开发范围**：在 T2.2.1 的扩充评测集上跑分，记录结果到 `docs/benchmarks-m2.md`；
  **达不到 7/8 就分析原因**：是定义仍不清（回 T2.2.1）还是模型能力不足（记为待决问题）。
- **明确不做**：不为了刷分而把用例改简单。
- **依赖**：T2.2.2、T2.2.3
- **交付物**：`docs/benchmarks-m2.md` 新增标签回归一节，含逐条结果与失败分析
- **验收命令**：评测脚本跑出 ≥7/8；未达标时 progress.md 里有明确的待决问题记录
- **涉及文件**：`docs/benchmarks-m2.md`、`spike/m2_bench.py`

---

## F3.1 — TTS 方言可行性摸底

### T3.1.1 候选调研：闽南语与粤语的本地 TTS  `DONE`
- **优先级**：high
- **目标**：回答「有没有」，不是「哪个好」。
- **开发范围**：调研本地可跑的 TTS 方案，每个候选记录：支持哪些方言、
  能不能纯本地跑（不依赖云）、运行时形态（是否又要背一个 Python 边车）、
  模型体积、许可与再分发义务、有没有公开的质量证据。
  **至少覆盖普通话、粤语、闽南语三档，泰语与英语一并记录。**
- **明确不做**：不实际下载模型（那是 T3.1.2）；不做质量主观评价。
- **依赖**：无（可与 M2 并行，一个改代码一个出文档）
- **交付物**：`docs/tts-survey.md`，每个候选一行，三栏不许留空：本地可跑 / 许可 / 证据来源
- **验收命令**：`test -s docs/tts-survey.md && grep -c '闽南\|台语\|Hokkien' docs/tts-survey.md`（至少命中 1 条）
- **涉及文件**：`docs/tts-survey.md`

### T3.1.2 实测最有希望的候选  `DONE`
- **优先级**：mid
- **目标**：把「据说支持」变成「实际跑通/跑不通」。
- **开发范围**：从 T3.1.1 选 1–2 个最有希望的，实际下载并合成一句测试语音；
  记录冷启动、RTF、内存、产物是否可听。**跑不通也是结论**，照实记。
- **明确不做**：不做多候选横比（那是选型，等 jason 拍板方向之后）。
- **依赖**：T3.1.1
- **交付物**：`docs/tts-survey.md` 补上实测数据；音频样本放 `spike/audio/tts/`（不入库）
- **验收命令**：`docs/tts-survey.md` 里出现「实测」小节且含具体数字
- **涉及文件**：`docs/tts-survey.md`

### T3.1.3 出 ADR-0005 草稿  `DONE`
- **优先级**：mid
- **目标**：给 jason 一份能据以拍板的材料，**不替他拍板**。
- **开发范围**：按现有 ADR 格式写 `docs/decisions/0005-tts-selection.md`，
  状态标「草稿，待拍板」；写清候选、证据、局限、以及**如果闽南语确实没有可用方案，
  M3 的三条可能走向**（只做普通话 / 推迟 M3 / 其他），每条列出代价。
- **明确不做**：**不选定方案**。这是产品决策，属于无人值守纪律里「不猜产品决策」那一条。
- **依赖**：T3.1.2
- **交付物**：`docs/decisions/0005-tts-selection.md`（草稿状态）；`docs/milestones.md` 的 M3 一节引用它
- **验收命令**：`grep -q '草稿' docs/decisions/0005-tts-selection.md`
- **涉及文件**：`docs/decisions/0005-tts-selection.md`、`docs/milestones.md`
- **风险**：这个 task 完成后会产生一个 BLOCKED 项（Q2：M3 怎么走），带着问题清单停下来问，符合无人值守纪律。

---

## F2.3 — 从跟进账本提升上来的正式 task

这几条原本在 `followups.md` 里，但规模已经超出「批量小修」——
按 pilot 的规矩提升为正式 task，单独走流程。

### T2.3.1 把 HTTP 调用抽象成可注入 transport  `DONE`
- **优先级**：high
- **目标**：让 `correct` / `label` 的错误分支能被**确定性测试**覆盖。
- **背景**：FU-4 + FU-11（codex 两次提到）。现在「垃圾 JSON / 缺字段 /
  HTTP 500 / 空 content / finish_reason=length / 批边界空白 / 中间批失败」
  这些分支**只能靠真实边车碰运气覆盖**，而它们恰恰是最容易出问题的地方。
- **开发范围**：定义一个 `Transport` trait（或函数指针），生产实现走 curl，
  测试实现返回固定响应；为上述每个分支写测试。
- **明确不做**：不引入 HTTP 客户端依赖（curl 子进程的选择不变）。
- **验收命令**：`cargo test transport` 全绿，且覆盖列出的每个失败分支
- **涉及文件**：`src/correct.rs`、`src/label.rs`

### T2.3.2 让基准脚本直接调用生产分类路径  `DONE`
- **优先级**：mid
- **目标**：消掉「基准 18/18 而生产 17/18」那个未定位的差异（现已同为 18/18，
  但差异的根因仍未知，两套代码还在各跑各的）。
- **背景**：FU-6 + FU-14。基准用的解析器更宽松、标签顺序也不同，
  两边的数字本来就不该拿来互相印证。
- **开发范围**：让 `spike/m2_bench.py` 调 `agentear` 的某个子命令
  （或直接跑 `cargo test --ignored`），不再自带一份提示词和解析器。
- **验收命令**：基准与生产报出同一个分数
- **涉及文件**：`spike/m2_bench.py`、可能需要给二进制加一个 `--classify` 子命令

### T2.3.3 curl 子进程的父进程墙钟超时  `DONE`
- **优先级**：mid
- **目标**：`--max-time` 只覆盖传输阶段，卡在 stdin 交互时不保证。
- **背景**：FU-5（codex Medium）。长录音分批后一次纠错要起 N 个子进程，
  任何一个卡住都会拖住整段。
- **开发范围**：父进程侧的 deadline + 到点 kill/wait；stdin 写入与
  stdout/stderr 排空并发进行（现在是先写完再读，有背压窗口）。
- **验收命令**：新增测试模拟一个不读 stdin 的子进程，断言父进程在 deadline 后返回
- **涉及文件**：`src/correct.rs`、`src/label.rs`

### T2.3.4 last-good 持久化  `DONE`（PR #27，commit 2b3b916）
- **优先级**：low
- **目标**：进程重启后 last-good 缓存为空；若编辑器截断文件后崩溃或断电，
  启动加载只能退回内置默认表。
- **背景**：FU-12（codex Low）。
- **开发范围**：每次成功解析并清洗后，原子更新 `terms.json.bak`；
  主文件坏且内存缓存为空时，验证并读备份。**解析失败时绝不更新备份**。
- **验收命令**：`cargo test terms` 新增「主文件坏 + 无内存缓存 → 读备份」用例
- **涉及文件**：`src/terms.rs`

---

## F2.4 — 让 M2 真的能用（**待 jason 拍板，不擅自开工**）

2026-09-03 他问「工具做好了没」时暴露的问题：**M2 的 3421 行代码全在 main 上、
20 个 PR 全绿，但最新发布版 v0.3.1 一行都不包含**；就算从源码构建，
要用术语纠错还得手动跑 `setup-llm.sh` 下 7.8 GB、再开一个终端挂着
`serve-llm.sh`。**那不是工具，是开发环境。**

下面三条是我的建议，但**每一条都涉及架构或产品决策，必须他点头才动**：

### T2.4.1 守护进程管理边车生命周期  `DONE`
- **待决**：Rust 守护进程要不要负责拉起/守护 mlx-dspark 子进程？
  这会改变 ADR-0002 划的那条线——那里说边车是「独立进程、可独立重启、
  独立崩溃」，而自动拉起意味着守护进程要管它的生死。
  **算不算违反那条约束，是架构决策。**
- **若拍板做**：菜单显示边车状态；启动时按需拉起；崩溃后重试有上限；
  退出时不留孤儿进程。

### T2.4.2 发 v0.4.0，带上 M2  `DONE`
- **jason 2026-09-03 拍板**：等 T2.4.1 做完再发；**不打包 7.8 GB 模型**，
  「最差手动启动或者静默失败继续」，未来适配上下游组件协作。
- 因此 `llm_start_command` 默认为空 = 只连不拉；用户要自动拉起就自己配路径。
- **✅ 已发布**：v0.4.0（PR #30）。其后又发了 v0.4.1（知识库投递默认开，#33）、
  v0.4.2（全文检索，#35）、v0.5.0（ASR 引擎可切换，#45）、
  **v0.6.0（通话链路，本 PR）**。

### T2.4.3 routes 的下游投递  `DONE`
- **jason 2026-09-03 拍板**：先做投递，三件事一起做完；
  L2 索引（rusqlite + SQLite FTS5）排在这三件之后，不并进来。
- 落地：
  1. `KbSink` trait + `FileSink`（`src/kb.rs`）——渲染 ADR-0003 §3.3 的
     front matter，按 front matter 的 `id` 去重，正文改了也不会堆出第二篇
  2. 重试队列 `routes/.pending/`（`src/deliver.rs`）——**入队在投递之前**，
     所以「投递中途被杀」不会留下没人管的记录；`MAX_ATTEMPTS = 10` 后放弃并标 `failed`
  3. `--replay-kb` 从 `routes/` 全量重建，幂等，可反复跑
- 两处偏离 ADR-0003 §3.3 的地方已回写进 ADR §7.1：`journal` 走 `kb/private/`
  独立子树；`unknown` / `command` 不投递；`kb/index/tags.md` 推迟到 L2。
- **没做的**：组织档（memos）适配器、L2 索引、L3 行动层。

### T2.4.4 L2 索引  `DONE`
- `src/index.rs`：rusqlite + SQLite FTS5，`--search` / `--reindex`。
- **分词方案实测选出**（同语料同查询，2026-09-03）：

  | 查询 | unicode61 | trigram | **unigram（选中）** |
  |---|---|---|---|
  | 「录音」2 字 | 0 | 0 | **2** |
  | 「录音 设备」跨词边界 | 0 | 0 | **1** |
  | 英文前缀 `know*` | 1 | 0 | **1** |
  | 英文任意子串 `know` | 0 | **1** | 0 |
  | 索引体积 | 1.0× | 3.5× | 1.55× |

  **换分词方案要重跑这张表**，别凭印象换。
- 前置假设已证实：`rusqlite` 的 `bundled` 随包 SQLite **3.50.2**，
  FTS5 / unicode61 / trigram / porter 全部可用。
- 硬约束满足：索引能从 `kb/` 全量重建，`derived/index.sqlite` 可随时删。
- **踩过的坑**：只给正文切分、忘了标签 → 中文标签整个搜不到，
  而英文标签恰好没事，所以只测 `esp32` 时溜过去了。

### T3.3.1 TTS 模块（三语，交给 Arm）  `ASSIGNED`
> ✅ **V1 HTTP 服务已合入 main**：`services/tts/{server.py,check_macos.py,test_server.py,
> ACCEPTANCE.md,README.md}`，PR #41 `feat(tts): add local macOS TTS service`
> （merge `5fe1302`，2026-09-09）。形状与下面写死的验收一致：
> `POST /speak {text,lang}` → `audio/wav`、`/health`、`/voices`、中英泰走 macOS `say`，
> 16 条测试通过，三语实测 200 + 22050 Hz 单声道。
> ⚠️ **为什么仍是 `ASSIGNED` 而不是 `DONE`**（体例照 T3.4.1 那段）：
> `ACCEPTANCE.md` 开头明写 **「No Rust integration or GitHub submission performed」** ——
> 守护进程里没有任何一行调用这个服务，用户装上 AgentEar 也听不到声音。
> **剩下的是 Rust 侧集成**：配置项 `tts_url`、调用时机、失败降级。
> 不要因为「代码在 main 上」就把它读成已交付。
>
> 🔄 **2026-09-14 更新（本轮实况，别照上面那段的字面读）**：Rust 侧集成**已经做了**，
> 但**藏在通话开关后面**——`src/talk.rs::HttpTts` 经 `tts_url`（默认 `:8765`）调
> `POST /speak`，守护进程在 `talk_enabled` 打开时用 `answer_out_loud()` 调它，
> 失败只记日志不挡上屏；另有 `--say <文字>` 与 `--ask <文字>` 两个 CLI 入口。
> **仍然不是 `DONE`**：① 默认关（**不开 `talk_enabled` 的用户还是听不到声音**）；
> ② 后端换了——默认从 macOS `say` 变成 **VoxCPM2-4bit**（`say` 降为保底，见
> `backends.py` / `server.py --backend`）；③ 人耳验收没做（Q3 仍开着）。
> 换句话说：**「有没有 Rust 集成」这一问已经答完了，「用户装上能不能听到」还没答。**
- **任务书**：[`docs/tasks/tts-module-arm.md`](../tasks/tts-module-arm.md)（中/英/泰三语）
- 目标链路的最后一段：拿到文字 → 用声音说出来，**中/英/泰可切换**。
  「大模型回答问题」那一段依赖外部服务，**本项目不做**。
- **接口先定死**：独立 HTTP 服务，`POST /speak {text, lang}` → `audio/wav`，
  加 `/health` 与 `/voices`。形态照 LLM 边车（配置里一行 `tts_url`）。
  这样 V2 换实现时 V1 的工作不白费。
- **V1 用 macOS 内置 `say`**：已实测目标机器上三语音色都在 ——
  `Tingting`(zh) / `Samantha`(en) / **`Kanya`(th)**。零安装、零下载、全离线。
  **这一档保留为「零依赖保底」**（ADR-0007 §4.2 的能力矩阵里，TTS 的零依赖默认实现）。
- **TTS 的第二档引擎已经有着落了（2026-09-08 更新）**（⚠️ 这里的「第二档」指 TTS 实现档位，**与产品 V1/V2 双工档位无关**，别混）：`VoxCPM2`（经 `speech` CLI），
  🔄 **2026-09-14 更正载体**：实际落点是 **`mlx-community/VoxCPM2-4bit` 经 `mlx-audio`**，
  跑在 `services/tts/backends.py::VoxCpm2Backend` 里（`--backend voxcpm2`，默认），
  **没有走 `speech` CLI**。ADR-0005 顶部与 ADR-0007 §6 都是按这个落点写的。
  Apache-2.0、48 kHz、支持泰语。实测见 [`benchmarks-m3.md`](../benchmarks-m3.md) §6：
  语言识别判定泰语 99.3%，**jason 试听结论「还可以」**。
  **接口一个字不用改**——`POST /speak {text, lang}` → `audio/wav` 照旧，
  只是内部从 `say` 换成 `speech speak --engine voxcpm2`。
  这正是当初把接口先定死的价值。
- ⚠️ **中文音色注意**：instruct 写 "energetic young woman" 会尖锐得没法听（踩过）。
  中文默认用沉稳档，别用兴奋档。
- V1 合并后再开 V2。
- 约束：**不许联网**（隐私红线，排除所有云 TTS）；**独立进程**（ADR-0002）。

## F3.4 — 实时语音对话 V1（ADR-0007）

> **顺序不能改**：T3.4.0 是硬门槛。它没过，后面四个都不用开工。
> 理由与 M0 相同——**高风险未知项前置**，别在一个做不到的目标上写三周代码。
>
> 🔄 **2026-09-14 实况**：T3.4.0 的判定是「**不通过，但没到推翻方案的程度**」
> （唯一卡点是 AEC 残留单字），所以 T3.4.1 / T3.4.2 / T3.4.3 事实上已经开工，
> 链路也跑通了。**但那不等于门槛过了**：AEC 事件级自触发仍是 5/5，
> 三条出口判据一条都没测。T3.4.2 标 `IN_PROGRESS` 而不是 `DONE` 就是这个原因。

### T3.4.0 实时链路可行性 spike（**硬门槛**）  `DONE`（判定：不通过，但方案不推翻）
- **优先级**：critical
- **目标**：在写任何正式代码之前，证明 V1 的四条验收阈值**能达到**。
  达不到就回头改方案，而不是改阈值。
- **形态**：**丢弃式 spike**，照 M0 的规矩来——脚本 + `speech` CLI，
  不进主工程，用完即删，只提交结论。
- **要测的四件事**（全部来自 `asr-selection.md` §4，不是新发明的）：

  | # | 判据 | 阈值 | 为什么它是门槛 |
  |---|---|---|---|
  | 1 | AEC 自触发率 | **= 0** | TTS 从扬声器回灌进麦克风，ASR 会自己跟自己聊 |
  | 2 | 打断响应 | **< 300 ms** | 超了就不是「可打断」，是「等它说完」 |
  | 3 | 误打断 | **< 1 次 / 10 分钟** | 太灵敏会把换气当成插话 |
  | 4 | 并发常驻峰值内存 | **≤ 10 GB** | V1 的产品档位承诺 |

- **必须测两种声学条件**：外放（AEC 真正受考验）+ 耳机（对照组）。
  只测耳机等于没测 AEC。
- **明确不做**：不实现 trait、不改 `src/`、不做 UI。只回答「能不能达标」。
- **验收命令**：产出 `docs/benchmarks-m3.md` 的新章节，四行阈值表填满，
  每行标 PASS/FAIL 并附测法；外放与耳机两组数据都在。
- **风险/回滚**：**如果 AEC 或打断不达标**，V1 要改形态——
  比如退回「强制耳机」或「按住说话（push-to-talk）」。
  这两个都是产品降级，**必须让 jason 拍板，不要工程师自己选**。
- **✅ 已完成 2026-09-08，结论见 [`benchmarks-m3.md`](../benchmarks-m3.md) §7**：
  内存 2.04 GiB PASS；VAD 检出延迟 ≈0 ms 部分 PASS；
  **AEC 部分通过**——5 轮有效重测，VPIO 抑制 20.4 dB，
  **内容级自触发 0/5**，但**事件级 5/5**（残留单字）；误打断未测。
  **唯一卡点是单字残留，解法明确，属 T3.4.2 实现工作。**
  ⚠️ 第一批三轮数据因扬声器被静音而作废，重测加了「对照组必须复现 TTS 内容」的守卫。
- **两处架构影响**：
  1. **AEC 走 CoreAudio VPIO，不走 speech-swift** —— 后者的 AEC
     只有 Swift 库、没有 CLI/HTTP 出口（实测端点 404）。
  2. 端到端打断延迟与误打断率**转为 T3.4.2 的出口判据**，spike 里测不了。
- **spike 脚本存档**：`spike/t340-realtime/vpio_test.swift`

### T3.4.1 引擎适配层  `DONE`（代码已发布 PR #44 / v0.5.0）
> ⚠️ **`DONE` 在这里只代表「代码已交付并发布」，不代表验收项全满足**：
> 下面那条「必须同时定死 ADR-0007 §4.4 编排职责」**没有做到**，债转 T3.4.2。
> 之所以不退回 `IN_PROGRESS`：引擎层本身已随 v0.5.0 到用户手里，
> 而未决的是会话编排——它属于 T3.4.2 的地界，压在这里只会两个 task 一起卡着。
- **依赖**：T3.4.0 已跑完；本 task 不依赖 AEC 结论，可直接开工
- **目标**：把 `src/asr.rs:118` 已经在做的「按语言分派后端」提升为显式接口，
  让引擎可以配置切换而不重编译。
- **按 ADR-0007 §4.2 的能力矩阵做，不是「每个 trait 都两个实现」**：
  ASR/TTS/LLM 支持多后端；VAD/TurnCompletion 按会话模式声明；
  **AecEngine 当前只有一个候选，先不抽象**。
- ⚠️ **这一步必须同时定死 ADR-0007 §4.4 的编排职责二选一**
  （自主编排 vs 委托 `/v1/realtime`）。两个状态机抢轮次语义是设计缺陷，
  不能留到实现时再说。
- **明确不做**：不换默认引擎。既有用户链路（SenseVoice + whisper 泰语）保持默认。
- **验收命令**：`cargo test engine_registry` 全绿，含「配置切换后端不重编译」
  与「未知后端名给出可操作错误」两条用例。
- **涉及文件**：`src/engine.rs`（新，518 行）、`src/asr.rs`、`src/config.rs`
  ⚠️ 落成**单文件模块** `src/engine.rs` 而不是 `src/engine/` 目录——
  当时只有 ASR 一族需要抽象。TTS / LLM 也抽象时再拆目录。
- **✅ 已完成 2026-09-08（PR #44 → v0.5.0 / PR #45）**：
  - `AsrEngine` **trait**（`src/engine.rs:77`）+ 后端枚举 `AsrBackend`（同文件 `:37`）；
    两个实现：`BuiltinEngine`（SenseVoice + whisper 泰语，**仍是默认**）
    与 `SpeechSwiftEngine`（走外部 `speech` CLI，Qwen3-ASR），
    `--asr-backend` / 配置项可切，**不重编译**；未知后端名给出可选值列表，不静默退回。
  - 实机验证同一条泰语音频：builtin 把英文词整段音译成泰文，
    speech_swift 保留 `merge pull request` 的拉丁拼写。
  - **顺带修掉一个自己引入的 bug**：`--asr-backend x --transcribe y` 会让
    `args[1]` 不再是子命令，**程序静默变成守护进程**。五个子命令改为位置无关并加回归测试。
    这正是 T3.4.5（改用 clap）的由来。
- ⚠️ **§4.4 的编排职责二选一（自主编排 vs 委托 `/v1/realtime`）在本 task 里没有拍板** ——
  引擎层做的是原子调用，会话编排一行没写。**这笔债转由 T3.4.2 偿还，不能再往后拖。**

### T3.4.2 通话会话层  `IN_PROGRESS`（2026-09-20 最新状态见下，历史记录保留在本条后半段）

#### 🔄 2026-09-20 当前状态（读这一段就够，下面是历史存档）

- 🛑→✅ **发现并修了一个比措辞严重得多的真 bug：播放期间按录音键在守护进程里实际打不断**。
  2026-09-19 那轮 codex 复查更深一层发现：`worker()` 主循环是**唯一**读
  录音键事件（`rx.try_recv()`）的地方，而 `finish()` 里对话模式的
  `answer_out_loud(...)`（LLM→TTS→`play_blocking()`）是**同步调用**，
  会一路阻塞到这一轮播完才返回。于是播放期间用户按键，事件确实被送进了
  channel（按键回调在 AppKit 主线程，`tx.send()` 不阻塞），**但 worker
  线程压根没机会回到循环顶部去读它**——只有播完之后才会处理，`Begin`
  分支里的 `talk::stop_playback()`（`main.rs:922`）因此从未在真实播放中
  被触发过。**之前那条 `stop_playback_really_cuts_the_sound_short` 单测
  一直是绿的，是因为它在一个单独 spawn 的线程里跑 `play_blocking`，
  跟真实守护进程"一个线程既读按键又播音频"的接线不是一回事**——测试
  通过不代表真实链路通。
  这个发现同时解释了一件事：`session.rs::begin_turn()` 对 `Phase::Speaking`
  的处理分支（`barge_in()`，还有专门为它写的用例）**一直是对的**、
  `talk::PLAYING`/`stop_playback()` 也一直是跨线程安全的设计——问题只在
  `main.rs` 里从没真的把播放挪到另一个线程上，这些设计因此从未被真实触发。
  **修法**：把 `finish()` 里那次 `answer_out_loud(...)` 调用改成
  `std::thread::spawn` 起一个线程去跑，worker 循环立刻能回到
  `rx.try_recv()`。用户按键时 `Begin` 分支会先 `stop_playback()` 掐掉
  这个线程正在放的声音，再开新一段录音——这正是代码里那段"打断优先于
  开始录音"的注释原本假设的行为。
  ⚠️ **验证边界要说清楚**：这个改动经过 `cargo build` + 全量 `cargo test`
  （292 passed）+ 静态读代码确认根因与修法对应，但**没有在真实守护进程上
  用物理键盘按键实测过**（本环境没有可用的麦克风/TCC 权限/真实 TTS 边车）。
  建议 jason 在自己机器上手动过一遍：对话模式下让它念一句长回答，播放中
  按一下录音键，确认声音立刻停、新一段录音立刻开始。
- ✅ **拍板：V1 打断方式定为推键式长期终态**，不补 VAD 自动打断/双讲。
  连带：AEC 事件级残留单字、误打断率这两条**不再是 V1 必做/必测项**。
- ✅ **实测了一个内部区间**（不是完整的端到端判据，见下方说明）：
  `stop_playback()` 调用到 `play_blocking` 确认返回，本机 5 次
  3.2–24.4ms，远低于 300ms。⚠️ **这不等于"物理按键到声音真的停"的完整
  端到端延迟**——键盘事件分发到 channel、channel 分发到 worker 循环
  调用 `stop_playback()`、以及"线程返回"到"扬声器真的没声音"之间的
  音频设备延迟，都不在这次测量范围内。真正的端到端延迟仍未测。
  ⚠️ **之前这里写过"键盘事件分发有 `hotkey.rs` 判定窗口量级 500ms"这句
  话，是错的，已删**：`classify_tap()` 是纯函数，按键一松开立刻分类，
  不会为了判断单击/双击而等待；`DOUBLE_TAP_MAX_MS`（500ms）只是"两次
  敲击之间的间隔阈值"，不是每次按键都要经历的等待窗口。
- ⚠️ **"StreamCheckpointPolicy 不需要了"——这个结论 2026-09-19 当天先写
  下、又在一轮 codex 评审后撤回了，是个真实犯过的错，留档不删**：
  推键式录音确实有明确起止点，但**提交前的录音（最长可达 300 秒）在
  进程崩溃时会整段丢失**，不是"零丢失"，反而比 `StreamCheckpointPolicy`
  想解决的"有界丢失"更差。详见 ADR-0007 §4.5、§8 第 7 条——**这是一个
  待 jason 决定的真实产品风险**（接受 / 缩短录音上限 / 做某种增量落盘），
  不是已经关闭的 checklist 项。
- **持久化没换策略**：通话还在走既有的快捷键录音落盘路径
  （`BatchCommitPolicy`），风险见上一条。
- **没有「起/停一次通话」的入口**：v0.7.0 起菜单里能切**模式**（默认输入法，
  换到对话模式点一下即可、立刻生效），但那是**持久设置**，不是一次会话的开始与结束。
  **讨论中，jason 还没拍板**——我提出的看法是：现在每轮对话互不记忆，
  "一次通话"的边界暂时没有实质内容去填，真正的痛点（边车常驻占内存）
  用空闲超时释放就能解决，不需要专门的"打电话"式入口；但这只是我的建议，
  不是已经定下来的结论。

<details>
<summary>历史存档：2026-09-14 的原始记录（保留，不要据此判断当前状态）</summary>

> ⬅️ **承接 T3.4.1 的未偿验收项**：ADR-0007 §4.4 的编排职责二选一
> （自主编排 vs 委托 `/v1/realtime`）。T3.4.1 标了 `DONE` 但**没做这件事**，
> 债记在那条的注脚里，**实际要在这里还**。
> ✅ **2026-09-14 已还：选 A（R2 自主编排），代码落在 `src/session.rs`。**
> ❌ **不要因为它是 `IN_PROGRESS` 就以为快完了**：下面三条出口判据**一条都没测**，
> 而且**仍然没有「起/停一次通话」的入口**——v0.7.0 加的是**模式菜单**
> （输入法 / 对话，默认输入法），它决定按一下键会不会出声，
> **不是**打一次电话那种「接通 / 挂断」的生命周期入口。持久化也还没换策略。
- 打电话式入口（起/停一次通话）、VAD 打断、语言随时切。
- **持久化走 `StreamCheckpointPolicy`（有界丢失），不是零丢失** ——
  见 ADR-0007 §4.5，别把文件导入的语义套到实时流上。
- **原有录音输入法形态继续保留**，不被通话取代。
- **验收命令**：`cargo test session_state` 全绿；含「通话中切语言」
  「打断后恢复行为一致」两条用例。
- **⚠️ 承接两笔债，开工第一件事就是它们**：
  1. **ADR-0007 §4.4 的编排职责二选一**（ADR 倾向 A 自主编排，但**明写了不能默认已定**）。
  2. **T3.4.0 留下的唯一卡点**：VPIO 开启后内容级自触发 0/5、
     **事件级仍 5/5**（实测残留**单字**）。解法已明确——最小语音时长 + 能量门限 +
     播放期 gating——但**要在这里实现并实测到自触发率 = 0**。
     ⚠️ 别把「1–5 字」当成实测上限：**实测到的全是单字**，
     那 1–5 字是解法里提出的**过滤类别**，不是测出来的边界。
- **另两条出口判据也在这里收**（T3.4.0 测不了，见 `benchmarks-m3.md` §7.6）：
  端到端打断响应 **< 300 ms**、误打断 **< 1 次 / 10 分钟**，
  **外放与耳机两组都要测**。

#### ✅ 2026-09-14 实际做到了什么

> 数据与脚本：`scripts/talk-e2e.sh`；Rust 侧 `src/session.rs`（462 行）+ `src/talk.rs`（754 行）。

- **债 1 已还（选 A 自主编排）**：`src/session.rs` 独占会话状态机——
  相 `Idle/Listening/Thinking/Speaking/Failed`；
  `begin_turn` / `finish_listening` / `turn_ready` / `speaking_done` / `barge_in` /
  `set_lang` / `fail` / `hang_up` / `turn_elapsed`。R0 只提供原子的 LLM / TTS 调用，
  **没有用上游的 `/v1/realtime`**。代价照 ADR-0007 §4.4 写的承担：轮次语义自己实现。
- **「语言随时切」落地**：`set_lang` 在通话中任意时刻生效，**Thinking 相拒绝**
  （上一轮的提示词已经发出去了，此刻改语言只会让回答语言与用户预期错位；
  理由写在 `src/session.rs` 的文档注释里）。
- **打断落地的是「推键式」，不是 VAD 自动打断**：守护进程录音键路径
  **先 `talk::stop_playback()` 掐掉正在播的回答再开麦克风**。
  ⚠️ **别把它读成债 2 已还**——AEC 那一格没动，见下。
- `cargo test session_state` **10 条全绿**（含验收点名的「通话中切语言」
  「打断后恢复行为一致」）；`cargo test` 全量 **239 passed / 0 failed / 6 ignored**。
- **推键式链路本身可无人值守验证了**：`agentear --talk-turn <wav> --lang zh|en|th`
  跑的是守护进程**同一个** `answer_out_loud`（ASR → 会话 → LLM → TTS → 播放），
  三语各跑通一次（`docs/benchmarks-talk.md` §4.3）。
  ⚠️ **不要把它当成「推键式已经验收」**：它绕过麦克风与按键，
  真实按键路径（TCC、CGEventTap、录音落盘）仍然只有人肉能验。
- **打断机制有用例**：`cargo test -- --ignored stop_playback` 真掐一段 5s 音频
  （需要能出声的环境，故 `#[ignore]`）。这测的是 `stop_playback` 本身，
  **不是**「按下键 → 掐掉 → 开新录音」那条端到端路径。

</details>

**2026-09-19 补的测量（准确表述，纠正过一次；2026-09-20 又删了一处编造的数字）**：
上面那条测试加了精确计时——从调 `stop_playback()` 到 `play_blocking` 确认
返回之间的时长，本机连跑 5 次：**3.2 / 13.7 / 24.4 / 9.2 / 9.9 ms**。
⚠️ **这只是"我们代码能控制的那一段"，不是完整的端到端打断延迟**——
键盘事件分发到 channel、channel 分发到 worker 循环调用 `stop_playback()`、
以及"线程返回"到"扬声器真的没声音"之间的音频设备延迟，都不在这次测量
范围内，仍未测。方差主要来自 `play_blocking` 的 20ms 轮询间隔，不是 kill
系统调用本身的抖动。⚠️ **之前这里写"键盘事件分发有 `hotkey.rs` 判定窗口
量级 500ms"是编造的**：`classify_tap()` 按键一松开立刻分类，不等待；
`DOUBLE_TAP_MAX_MS` 只是两次敲击之间的间隔阈值，不是单次按键的处理延迟。
详见 `src/talk.rs::direct_stop_returns_playback_thread_quickly` 的注释
（2026-09-20 改名，原名 `stop_playback_really_cuts_the_sound_short` 暗示
它测的是 V1 打断机制本身，实际只测了 `stop_playback`/`play_blocking`
这两个原语，见下方 §"打断线程化"）。

### T3.4.3 mock LLM（查天气）  `DONE`（2026-09-14）
- 一个「查天气 → 语音说出来」的最小实现，用途有二：
  证明 ASR→LLM→TTS 整条链路通；给集成方一个可照抄的 `LlmEngine` 样例。
- ⚠️ **文档必须写明它不是产品功能**，否则会被当成智能助手来评价。
- **验收命令**：端到端脚本：说「今天天气怎么样」→ 得到语音回复 + 文字，全程离线。
- **✅ 已完成 2026-09-14**：
  - `LlmEngine` 两个实现都在 `src/talk.rs`：`OpenAiCompat`（任何 OpenAI 兼容端点）
    与 `WeatherMock`（零依赖、写死回答）。⚠️ **mock 必须显式配置**
    （`talk_llm_engine: "mock"`）才启用——默认成 mock 会让人以为模型在跑。
  - **验收命令实跑**：`scripts/talk-e2e.sh`，**中 / 英 / 泰三语都跑通**，
    一步一步打印「听到 → 回答 → 语音文件」（文字与语音两路都有）。
    回环验证：中文合成音频送 SenseVoice 得回「今天青迈天气还不错，最高32度。」，
    泰语送 whisper(th) 得回「วันอีกที่เชียงใหม่อากาศดีอุณหภูมิ 32 องศา」。
  - 交付的不只是 mock：真正的默认引擎是 **MiniCPM5-2B-4bit 边车**
    （`scripts/serve-talk-llm.sh`，`:8794`），mock 是零依赖的样例路径。
  - ⚠️ **天气那句话是本地写死的场景，不是天气接口**（ADR-0007 §4.6）。
    它只用来证明链路通。**接真实天气源是集成方的事**，别把它写进产品能力。
  - ⚠️ **泰语在模型能力边界外**：MiniCPM5-2B 模型卡只声明 en/zh，
    实测能听懂泰语、也能产出泰语，但提示词不钉死语言时会用中文回答。
    **不要把泰语质量写成与中英同级。**

### T3.4.4 集成接口与文档  `BLOCKED`（等 T3.4.2 收口）
- `agentear serve`（OpenAI 兼容 HTTP/WS）+ `agentear mcp`，
  对应 jason 要的两种集成方式：配自己的模型、代码级接进别的 agent。
- **交付物必须含说明文档**，不然「预留接口」等于没留。
- **验收命令**：文档里的示例请求能原样跑通。

---

## F3.5 — 选型补课（与 F3.4 并行，不阻塞）

### T3.4.5 命令行参数改用 clap  `TODO`
- **为什么**：加 `--asr-backend` 时踩到——子命令识别依赖 `args[1]`，
  参数放前面会让程序**静默变成守护进程**（无报错，用户只看到菜单栏帮助）。
  已改成位置无关，但那仍是个手写的简化解析器。
- **它现在挡不住的**：值恰好等于另一个选项名（`--classify --search`）、
  重复子命令、多余参数。`args_after` 靠 `GLOBAL_FLAGS_WITH_VALUE` 白名单剔除
  全局选项，**加新全局选项时忘了往那张表里加就会重现同类 bug**。
- **验收命令**：`cargo test cli_` 全绿，含「值等于选项名」「重复子命令」
  「未知参数被拒绝」三条用例。

### T3.4.6 speech_swift 的资源预算拍板  `DONE`（jason 2026-09-08 选 A）
- **冲突**：Qwen3-ASR 1.7B 实测单次峰值 RSS **2.43 GiB**
  （`benchmarks-m3.md` §3.1），而 `CLAUDE.md` 与 ADR-0001 对 **ASR 侧**的
  要求仍是 **≤2 GiB** —— M2 的预算放宽写明「只针对 LLM 这一项」。
- ⚠️ **「可选而非默认」只避免了升级回归，不构成资源标准的豁免。**
  这一条是 codex 评审指出来的，我原先漏了。
- **要 jason 二选一**：
  1. 为可选高资源档单独声明预算与准入条件（改 `CLAUDE.md` 的措辞）
  2. 换一个满足 ≤2 GiB 的配置并实测 —— 0.6B 约 1.2 GiB，
     但三项指标都退（`benchmarks-m3.md` §2.1.1），要接受这个代价
- **✅ 拍板结果：选 A —— ASR 侧改为分档**（`CLAUDE.md` 已改）：
  默认档 ≤2 GiB（builtin）、高资源档 ≤4 GiB（用户显式启用的可选后端）。
  **准入三条**：必须用户显式启用、必须不随包分发、峰值 RSS 必须实测入库。
  `speech_swift` 的 2.43 GiB 落在高资源档，三条都满足。
- ⚠️ **分档 ≠ 取消约束**：默认路径的 ≤2 GiB 一步没退，
  没装外部依赖的用户内存占用与 v0.4.2 完全一致。
- ⚠️ **这不等于批准换默认引擎** —— 那要看中英横比（T3.5.1），还没做。

### T3.4.7 可配置语音指令表  `DONE`（2026-09-14，v0.9.0）
- **要什么**（jason）：录一句 → 配成指令 → 以后语音里含这句就自动执行；
  而且**指令范围是开放的**（不只是「请用广东话」那几条）。
- **两段式落地**：**本地表先查**（`src/commands.rs`，前缀匹配、0ms、断网可用）→
  **没命中走 LLM**。开放式的句子本来就该由模型理解，指令表只认用户声明过的那几条。
  ⚠️ **这条分工要写清楚**，否则会被读成「指令表能听懂任意话」。
- **动作是封闭集合**（`builtin` / `open_url` / `http_post`）：**绝不执行 shell**，
  语音识别错一个字就等于在你机器上执行命令，且没有撤销键。
  `open_url` 只允许 http/https/mailto（`file:` 拒掉）。
- **入口**：菜单「打开指令表」、`--add-command` / `--add-command-wav`
  （录一句 → ASR → 写表）、`--commands`、**`--match-command`（干跑）**。
- **✅ 已完成 2026-09-14**：`cargo test` 238→**268**（新增 10 条指令表用例）。
  - ⚠️ **`--match-command` 当天就抓出一个真 bug**：槽位原取自**归一句子**，
    「帮我搜代码 talk.rs」→ 槽位变 `talkrs`（点没了、大写没了），
    而且**当时的测试把这个坏行为钉住了**（`rest == "rustasync"`）。
    现在槽位取自**原文**，URL 模板里的槽位按模板类型转义。
    **教训**：可干跑的入口本身就是一种验收手段，比人肉按键可靠。
- **还没做**：
  - **繁简归一没做**（无离线表）：`記一下` 与 `记一下` 算两条。别写成「都行」。
  - **没有「录一句 → 建一个 http_post 动作」的一步到位**：`--add-command-wav` 只定短语，
    动作还得用 `--action` 指定。
  - **误触发率未测**：短语短（如「打开」）时在正常对话里也会被抢走。
    要测就得攒一批自然语句统计误命中——还没做。

### T3.4.8 句子级流水线（边出边合成）  `DONE`（2026-09-14，v0.10.0）
- **要什么**（jason）：「三个能提速的都做一下」里的第 3 项——把首字延迟压下来。
- **做法**：LLM 侧 SSE 流式（`stream: true`）→ `SentenceSplitter` 切句 →
  逐句 `tts.synthesize` → 逐句 `play_blocking`。生产者在播放上一句时合成下一句。
- **✅ 实测**（同题各 3 次，M1 Max / 64 GB）：首字 **3.27 / 4.00 / 6.94s →
  2.27 / 2.29 / 2.60 / 2.61 / 2.78s**，且不再随回答长度增长；
  句间无可测空隙（墙钟 10.40s ≈ 首字 2.27 + 播 7.85）。明细见
  `docs/benchmarks-talk.md` §2.5。
- ⚠️ **总时长基本没变**（省的是等待）；**地板是 TTS 那约 2.4s 的固定开销**。
  **不许写成「压到 1 秒」**。
- ⚠️ **两条静默坑**（都实测到）：URL 漏拼路径 → 404 → 静默退回；
  退回时忘了喂 `on_delta` → 有文字没声音。**倒退分支必须有输出。**
- **还没做**：
  - **TTS 的固定开销没压**（约 2.4s/次）：要压得动边车那一侧
    （常驻预热、或让边车吐 PCM 而不是整套 WAV 重建）。
  - **打断延迟未测**：流式下打断会中止后续合成，但「按键到声音停」的
    端到端延迟仍是未测（与 T3.4.2 同一条判据）。

### T3.4.9 TTS 量化档可选（默认 4bit）+ 开箱默认音色库  `DONE`（2026-09-15，v0.11.0）
- **要什么**（jason）：「我电脑上就用 8bit，四五个 G 我能接受；但**发布的默认
  必须是 4bit**，普通人的电脑没这么大内存」。
- **✅ 已完成**：`setup-talk.sh --tts-quant 4bit|8bit`（默认 4bit）+
  `serve-tts.sh` 同档位开关；`tests/script_defaults.rs` 4 条钉子把
  「默认档只能是 4bit」「每档有自己的下载下界」钉住。
- **实测**（HF 权重体积 / `/health` 峰值 RSS）：
  **4bit 2.30 GB / 约 2.4–2.5 GB**；**8bit 3.22 GB / 3268 MB**
  （与上一轮的 3266 MB 复现）。
- ⚠️ **纠正一个数字**：8bit 的权重是 **3.22 GB 不是 2.3 GB**——
  2.3 GB 是 **4bit** 的文件大小，上一轮把它错当成 8bit 记下来了，
  并据此把它下进了 `vendor/models/talk/`（触发了打包体积事故）。已改正。
- ⚠️ **没有检出 4bit 与 8bit 的输出质量差异**：所以**不许**把 8bit 叫「音质档」。
  要更好的音色走 `make_voice.py` 换参考音频。
- **顺带修掉一个真 bug（比量化档本身更值钱）**：`serve-tts.sh` **从来没传
  `--voices-dir`**，于是**只要边车是被守护进程按配置拉起来的**，音色库就是空的
  → VoxCPM2 零样本克隆**每次合成随机换一个说话人**；
  而 v0.10.0 的句子级流水线把一次回答切成好几句、每句各发一次请求，
  于是「音色飘」升级成**一句话里换好几个人**。
  手工带 `--voices-dir` 起的那条边车是好的——两套起法行为不同，所以一直没被发现。
  现在：`assets/talk-voices/`（1.9 MB，VoxCPM2 自举生成的）入库，
  `setup-talk.sh` 装进数据目录（**已存在不覆盖**），`serve-tts.sh` 传目录并钉住
  `female_zh_02`，数据目录没有时回退到仓库里那份。
- **还没做**：
  - **人耳验收**：「换参考音频之后音色是否可接受」仍然只能由 jason 听
    （声学代理指标区分不出配置，见 T3.5.4）。
  - **没有菜单入口**：切量化档要改 `talk_tts_start_command`（带
    `AGENTEAR_TTS_QUANT`）。做菜单项就得处理「另一档还没下载」，
    那要一套按需下载 UI，**没做**。
  - **`make_voice.py` 没有一键入口**，命令行参数偏开发向。

### T3.4.10 向外动作的二次确认  `DONE`（2026-09-15，v0.12.0）
- **要什么**（jason）：「所有向外输出的内容（写 Notion、发邮件……）都要二次确认」。
- **判据**（`commands::needs_confirm`）：会不会把内容**送出这台机器**。
  `http_post` / `mailto:` **一定问**；打开网页默认不问（可给单条指令加
  `"confirm": true`）——**每次都问的确认会被按成肌肉记忆**。
- **两条确认路径**：短按录音键（<0.3s）/ 说「确认、对、好的」。
- **✅ 已完成**：`Pending` 待确认 + 过期 + 同时只留一条；问句**念出来**
  （对话模式走 TTS，输入法模式落 stdout/日志）；菜单栏 `❓`；
  CLI `--run-command [--reply]`（与守护进程同一个 `run_command_turn`）。
  `cargo test` 285→**295**。
- **实测**（本机真起了一个 webhook，逐条验）：只有「确认」和「好的，发吧」会发出去，
  且**发出去的 payload 与念出来的完全一致**；「别发 / 取消 / 今天天气怎么样 /
  我刚才确认过了吗」四条**一条都没发**。
- ⚠️ **`mailto:` 的执行分支没实跑**（会在本机打开邮件客户端）；验的是「它会问」
  以及 http_post 的完整链路。
- **还没做**：
  - **确认没有「只读一次」的词汇边界**：靠 7 字上限 + 否定前缀，
    是启发式而不是理解。要更稳得上 LLM 判意图（那就得等一次模型调用）。
  - **没有「撤销」**：发出去就发出去了。这一层只保证「发之前问过」。
  - **`command_confirm_secs` 之外没有别的策略**（比如「同一条指令 10 分钟内不再问」），
    多次重复同一动作会有重复确认。

### T3.4.11 动作回执 + 回答可追溯  `DONE`（2026-09-15，v0.13.0）
- **起因**：jason 人工测试后问「你把网址给我看看，**你别骗我啊**」。查下去发现
  `http_post` 两处报喜不报忧（丢响应体、不看退出码）。
- **✅ 已完成**：响应体留下来了并挑出回执（url/id/链接/截断原文）；
  退出码非零 → 报失败 + 带上服务端解释（`--fail-with-body`）；
  `open_url` 也查退出码；**通话回答正文进日志**（流式路径漏了它）。
- **还没做**：
  - **回执只印在 stdout / 日志里，没有念出来**。对话模式下用户听不到 URL
    （念一串 URL 也不好听）——要设计「短回执 + 详情去哪儿看」。
  - **没有把回执写进 routes/ 留档**：现在关掉终端就查不到「那次到底写到哪个页面」。
    应该像转写一样落一条记录。
  - **`open_url` 的退出码语义只覆盖「有没有交出去」**，不覆盖「网页能不能打开」。

### T3.4.12 边界固化：AgentEar = 语音前端，下游交给 Agent24  `DONE`（2026-09-15，v0.14.0）
- **拍板**（jason）：「AgentEar 的定位就是语音交互」——命令执行、确认 UI、
  回执展示、初始化配置、凭据管理**都不是它的本职工作**，那些嵌到 **Agent24** 里做。
  当前结果他接受，但要求：**有接口 + 最基础的逻辑验证**。
- **✅ 已完成**：
  - **ADR-0008**（`docs/decisions/0008-voice-frontend-boundary.md`）：
    职责切分表、接口契约 `agentear.proposal/1`、**对 Agent24 的 6 条诉求**、
    以及「明确不做」的清单。
  - `agentear --match-command <文本> --json`：输出结构化 proposal，
    **只提出、绝不执行**。`prompt` 收紧成「只有要确认时才存在」。
  - `scripts/agent24-standin.py`：**假宿主**，验证「问 → 显示 → 确认 → 执行 → 回执」
    这条路走得通，且它**故意不调 `--run-command`**（那等于把执行塞回 AgentEar）。
- **⛔ 在这条线上停止开发**（不要再提新功能）：
  - 初始化向导 / 凭据管理 / 回执界面 / 历史列表 → **归 Agent24**
  - 多轮追问（「发给谁？」）→ 先换模型再说，2B 记不住上下文
  - 已做出来的语音二次确认（v0.12.0）与回执读取（v0.13.0）**保留**：
    AgentEar 单独跑时仍然有用，也正是「概念上验证可行」的产物。
- **⏳ 等 Agent24 回答的 6 个问题**（ADR-0008 §5）：事件通道形态、热键归属、
  麦克风/播放归属、配置归属、TCC 权限归属、回执留档归属。

### T3.4.13 TTS 首包延迟：参考音色缓存（方案 A）  `DONE`（2026-09-19，`services/tts/refcache.py`）
> ⚠️ **改名**：原标题"参考音色缓存 + 分块流式"拆成两条——分块流式（方案 B）
> 工作量、风险都独立，另立 **T3.4.14**，未开工。这条只覆盖方案 A。
- **起因**：jason 实测对话模式「说完话到听到回答」要 7–12 秒，64GB 内存没帮上忙——
  排查确认是算力瓶颈不是内存瓶颈，方案见 **ADR-0009**
  （`docs/decisions/0009-tts-chunked-streaming.md`）。
- **✅ Phase 0（算清楚，真实测量不是估算）已完成**：monkeypatch 生成循环里的
  `feat_decoder.sample()` / `audio_vae.decode()` 做计时，拆出耗时构成：
  - 短句「现在几点了」总 5.17s：**参考音色上下文编码 2.86s(55%)** +
    后续 patch 生成 2.24s(43%) + VAE 解码 0.07s(1%)
  - 中句「天气」总 7.61s：参考上下文编码 2.80s(37%) + 后续 patch 生成 4.56s(60%) +
    VAE 解码 0.20s(3%)
  - **对照实验坐实真凶**：零样本（不给参考音色）首 patch 仅 0.17–0.22s，
    比带参考音色快 **13–17 倍**，且重复调用不会命中缓存变快——排除
    「MLX 图编译只发生一次」，确认是**每次调用都要重新把参考音频编码成
    latent 序列过一遍 `base_lm`**，纯粹是重复计算，不是文件 I/O（**纠正了
    `CLAUDE.md` 里 "重读参考 wav 可忽略"这条旧记录**，实际成本是计算不是
    I/O，量级是短句里最大的单项开销）。⚠️ **归因更正（Phase 2 落地后一轮
    codex 评审挑出来的）**：最初以为是"参考文本（465 字）"被重新处理，
    读源码核实后发现走的这条路径（Mode 3）**根本不读 `ref_text`**——真正
    被编码的是参考**音频**本身，男声参考 98.7 秒 vs 女声参考 10.9 秒，
    预热耗时 3.56s vs 1.05s 与音频时长同方向，详见 ADR-0009 §3.3。
  - **架构事实**：VoxCPM2 在 `mlx_audio` 里本来就是**逐 160ms patch 自回归生成**
    （`patch_size=4` × `decode_chunk_size=1920` / 48kHz），卡住流式的只是最后
    "全部生成完才一次性 decode + yield"这一步，代码里已有 `streaming_prefix_len`
    这类分块解码的雏形。
- **方案分两条，有优先级，不是二选一**（详见 ADR-0009 §4）：
  - **方案 A（缓存参考音色编码，优先做）**：`base_lm` 已经在用 `lm_cache`
    支持增量前向，理论上能把参考部分的编码只算一次、跨请求复用，
    预期短句 5.17s → 约 2.3s（降 55%）、不碰 HTTP 契约、不碰 Rust 播放侧，
    工作量比方案 B 小一个数量级。
  - **方案 B（分块流式，原计划，次优先）**：把已经存在的逐 patch 生成循环接到
    对外接口，省的是等待感、不是总运算量（跟 T3.4.8 句子级流水线同一类优化，
    只是发生在 TTS 内部、粒度更细）。需要新开流式端点（`/speak` 老契约不动）、
    Rust 侧播放与打断逻辑跟着改，粗估 5–9 天。
- **✅ 方案 A 的 Phase 1 spike 已完成**（`spike/t3413-tts-refcache/`，README 有完整记录）：
  **可行，但 `mlx_audio` 有个阻塞性 bug 需要打补丁**——`MiniCPMModel.__call__`
  的因果 mask 只支持单 token 续接缓存（自回归循环那种 `L=1`），没考虑"一次续接
  多个新 token、还要看得见缓存里 `offset` 个已有位置"这种场景（`L>1` 时），
  直接 `ValueError: broadcast_shapes`。补丁是标准的（mask 覆盖 `(L, offset+L)`，
  前 `offset` 列全通过、后 `L×L` 因果），十几行代码，打在我们自己包的那一层，
  不改 `mlx_audio` 本身。
  - **打完补丁实测**：两句不同新文本复用同一份参考音色缓存都生成成功，
    **总耗时降到约 1/3**（4.19s→1.24s、4.53s→1.38s）——**比 Phase 0 估算的
    55%/37% 更好**。⚠️ 建缓存本身要 3.91s（比对照组单次还贵），只有同一音色
    被连续复用才划算，边车要在启动时/首次用到某音色时建好并常驻。
  - **正确性**：F0 中位数四条样本都落在参考音色的男声带内，`afplay` 人耳抽样
    确认不是乱码；不是逐字节比对（扩散无固定 seed，方差是预期的）。
  - **没测的**（留给 Phase 2）：`instruct`（语气/方言）切换是否影响参考部分
    编码（决定缓存要不要按"音色×instruct"分粒度）、长文本行为、缓存内存占用、
    边车重启后重建耗时。
- **✅ Phase 2（生产化）已完成**：新文件 `services/tts/refcache.py`
  （`patch_multi_token_cache_continuation` / `build_ref_cache` /
  `generate_with_cache`），`backends.py` 接入 `VoxCpm2Backend._warm_ref_caches`
  （启动时同步跑完，见下）与 `_generate`（先试缓存，失败退回老路径）。
  - **落地形态是 jason 拍板的简化**：不做"按需建缓存"，**启动时无条件预热
    音色库里的全部音色**（现在就男声/女声两个）——默认选哪个都是热的，
    菜单切换也是热的；音色库以后变多，预热范围跟着自动覆盖，不用改代码。
  - **`instruct` 不影响参考部分编码，是读代码确认的**（不是猜的）：`instruct`
    只拼进"新文本"前缀，参考前缀的构造完全不碰它——缓存按音色分粒度就够，
    不需要再按"音色×instruct"细分（Phase 1 spike 里列的疑问，Phase 2 澄清了）。
  - **就绪语义 + 测试竞态，实现时才发现**：预热最初写在
    `self._startup.set_result(True)` 之后。⚠️ **这不是"抢计算资源"**——预热和
    请求处理本来就在同一条 MLX 线程上串行，顺序不影响谁先跑；真正的问题是
    ①`/health` 就绪不该早于缓存真的热了，②测试里 `finally` 会在 `__init__`
    一返回就立刻恢复 `sys.modules`，如果预热还在后台线程跑就是真实的跨线程
    竞态。改成预热同步跑完才 `set_result`，两个问题一起解决。
  - **缓存续接失败会把那个音色的缓存直接扔掉**（`self._ref_cache.pop`），
    不是只这一轮不用——否则一个确定性失败会让以后每次请求都先白跑一遍
    缓存路径再退回慢路径。`_generate` 捕获到异常就退回 `_generate_uncached`，
    不是 500——⚠️ **只覆盖"抛异常"，不覆盖 native 调用卡死/挂起**（那种情况
    走 `synthesize()` 自己的 timeout，不经过这条 fallback）。
  - ⚠️ **`patch_multi_token_cache_continuation` 是进程级永久类补丁，没有
    版本校验**——设计成行为严格超集（只在之前必崩的组合上生效，其余路径
    原样委托），但假设了 `mlx_audio` 0.5.1 的内部结构，升级版本时这些假设
    不会自动校验，是已知债，记在 ADR-0009 里。
  - **测试**：30 条既有测试原样不动全部通过（`_warm_ref_caches` 对没配置
    `voices_dir` 的场景第一行就短路，零风险）；新增 6 条 `RefCacheWiringTests`
    覆盖预热失败降级、缓存命中真的被调用、续接失败正确退回**且扔掉坏缓存**、
    扔掉后不会重复踩坑、缓存对象跨调用被复用而非重建、无音色库时整条分支
    跳过。`python3 -m unittest discover -s services/tts` **50 passed**。
    ⚠️ **这批测试测的是接线对不对，不是数学对不对**——`refcache.py` 复刻了
    `mlx_audio` 私有模型内部前向，漏掉一步的后果通常是"能出声但声音不对"，
    mock 测不出来，正确性依据是下面这条真实模型验证。
  - **真实模型 + 真实 HTTP 端到端验证**（不是 mock）：重启边车两次，日志
    确认两个音色都建了缓存（女声 1.05–1.16s、男声 3.56–3.66s）；`curl /speak`
    实测男声「现在几点了」**1.12–1.14s**、女声「今天天气怎么样啊」
    **1.22–1.34s**——和 spike 数字（1.24s/1.38s）基本吻合；`afplay` 人耳听过，
    `measure-f0.py` 确认男声 142.9Hz / 女声 206.0Hz 都落在各自声带范围内。
    ⚠️ **都是单次采样，不是多次重复测量**——量级对得上，不是统计意义上的
    确认。`agentear --say` 跑通一句 26 字长文本**一次**，7.51s 生成 8.48s
    音频（RTF≈0.89）——**只是一次 smoke test 成功**，没有未缓存基线对照、
    没有重复采样，不足以断言"不是回归"，只能说这一次没观察到问题。
- **还没做**（留给需要时再评估，不是缺陷）：缓存的内存占用没单独拆出来测；
  边车重启后重建两份缓存要 ~4.6s，没人反馈过边车启动慢，暂不处理；
  `patch_multi_token_cache_continuation` 没有版本校验；针对真实模型内部
  计算的集成测试没做（受限于现有测试架构不装 mlx）。

### T3.4.14 TTS 分块流式（方案 B）  `BACKLOG`
- **依赖**：无强依赖，但建议在 T3.4.13 落地之后再排——分块流式优化的对象
  是"后续 patch 生成"那一段，T3.4.13 落地后这段更短、更干净，spike 测量
  不会被参考音色那 2.8s 的固定成本污染。
- **要做什么**：把 VoxCPM2 本来就有的逐 160ms patch 自回归生成循环接到
  对外接口，攒够 N 个 patch 就分段 `audio_vae.decode()`、流出去，而不是
  等全部生成完才一次性返回——省的是"听到第一个字"的等待感，不是总运算量。
  详见 **ADR-0009** §4.2、§5.2。
- **明确不做**：不改 `/speak` 现有契约（ADR-0007 定死的），要流式得新开端点；
  Rust 侧 `CurlAudio`/`play_blocking` 与打断逻辑（`stop_playback()`）要跟着改，
  这部分工作量在 T3.4.13 落地前没有细化。
- **验收命令**：待定——第一步是 spike 验证分段 `audio_vae.decode()` 拼接出来
  的音频跟一次性 decode 逐样本比对有没有可听见的接缝杂音，这条过了才谈
  Phase 2/3 的验收标准。
- **粗估工作量**：5–9 天（spike 1 天判生死，Phase 2/3 各 2–4 天）。

### T3.4.15 打断修复的两条遗留（确认提示音同样打不断 + 退出时播放进程可能残留）  `BACKLOG`
- **背景**：T3.4.2 的"播放期间按录音键在守护进程里实际打不断"bug（2026-09-20
  修复，`main.rs` 里对话回答的 `answer_out_loud` 改成了 `std::thread::spawn`）
  修完之后，本地模型自评审（当轮 Codex 配额用完，走 Tier 2 兜底）又发现了
  两条**同类、但未修**的遗留——jason 2026-09-20 拍板：先记账、不阻塞发布。
- **遗留 1：`announce_pending()`（`main.rs:1452`，命令二次确认的"念问句"）
  同样是同步调用 `talk::speak(...)`**，和刚修的那个 bug 是同一类问题——
  worker 线程被这段播放卡住时，用户按键打断不了这句确认提示音。
  影响面比对话回答小（提示音通常很短），但原理相同，修法也相同（spawn 出去）。
- **遗留 2：`talk::PLAYING` 是 `static`，进程退出（菜单 Quit / 信号）时 Rust
  不会跑 `'static` 变量的 `Drop`**——如果退出那一刻恰好有 `afplay` 子进程在播，
  它不会被自动杀掉、`Player` 的临时文件目录也不会被清理。这个口子在 T3.4.2
  这次修复之前就存在（旧代码下同样可以在阻塞播放期间点菜单 Quit），
  不是这次改动引入或加重的，但没有任何现有关闭路径（`talk::shutdown_spawned()`、
  `talk::kill_spawned_pids_from_signal()`）覆盖它。
  ⚠️ **信号路径要小心**：`kill_spawned_pids_from_signal()` 之所以只做裸
  `kill(2)`、不碰 mutex，是因为信号处理函数里拿锁不是 async-signal-safe
  （可能跟被打断的代码死锁）。要堵这个口子，Quit（菜单，非信号上下文）可以
  直接调 `talk::stop_playback()`；信号路径需要仿照 `kill_spawned_pids_from_signal()`
  的做法单独记一份裸 PID，不能直接调 `stop_playback()`。
- **验收命令**：待定——遗留 1 可以照抄这次 spawn 的模式加单测；
  遗留 2 需要先确认怎么在测试里模拟"进程退出时还有 afplay 在播"这个场景。

### T3.5.1 中文/英文 ASR 横比  `PR_OPEN`（2026-09-25，分支 `feat/T3.5.1-asr-compare`）
- **为什么必须做**：`benchmarks-m3.md` 只测了泰语。
  换默认 ASR 影响**全部语种**，不能凭泰语结果推断 SenseVoice 该不该换。
- 用 M0 的中文语料，横比 SenseVoice vs Qwen3-ASR，分语种给 CER。
- **这是「换默认引擎」拍板的前置**，没有它 jason 无法决策。
- **产出**：[`benchmarks-asr-zh-en.md`](../benchmarks-asr-zh-en.md)。
  M0 只有一条伪参考样本，所以另外加了 FLEURS 中/英 + ASCEND 中英混合（各 n=60）。
  M0 的 sample02 也复跑了一遍对账：SenseVoice 25.08%，和当年的数逐字一致。
  结论：英文、混合两组 Qwen3-ASR 1.7B 更好；中文组主口径下未检出差异，
  剔除数字写法的干扰后变成 1.7B 更好（这是事后定的口径）。
  代价是每次多约 2s 冷启动，峰值 2.4–2.6 GiB，只能进高资源档。
  **Q4（换不换）仍待 jason 拍板。**

### T3.5.2 whisper 逐条 CER 入库  `READY`
- **为什么**：`RESULTS.md` 只存了汇总指标，导致
  Qwen3 vs whisper 的**配对检验做不了**（benchmarks-m3.md §4.1）。
- 用配置 D 的参数重跑 whisper，逐条 CER 入库，
  与已入库的 `qwen3-asr-2026-09-08.tsv` 做配对 bootstrap，报差值置信区间。
- **拿到区间之后**，才可以使用「未检出差异」「非劣」这类措辞。

### T3.5.3 逐组件许可证表  `READY`
- **为什么**：speech-swift 仓库代码是 Apache-2.0，
  **但模型权重各自授权**——同一批里 VoiceChat 11B 就是仅研究用途。
- 逐项列：Qwen3-ASR / VoxCPM2 / IndexTTS2 / Smart Turn / LocalVQE /
  FunctionGemma / PersonaPlex / VoiceChat 的代码许可、权重许可、
  再分发义务、商用限制。
- **未确认许可的组件不得进入默认方案。**

### T3.5.4 中文方言人耳验收  `READY`
- 样本已生成：`~/Desktop/agentear-tts-samples/voxcpm2-2026-09-08/`
- 要 jason 回答两件客观手段答不了的事（benchmarks-m3.md §6.2.2）：
  方言到底生没生效？台湾腔的优先级？
- ~~**台湾腔是缺口**，若是硬需求要评估引入 FireRedTTS3~~
  → **已降级（jason 2026-09-08）**：台湾腔不再是筛选条件，
  **FireRedTTS3 相应地从候选里移除**（引入它的唯一理由就是台湾腔）。
  VoxCPM2 的 9 种方言够用，不需要第二个 TTS 引擎。台湾腔记为已知缺口。

### T3.5.5 上游版本锁定与回滚流程  `READY`
- 锁到 commit + release 资产 SHA-256（tag 会被移动、asset 会被替换）。
- 保留已验证版本的本地副本或可复建脚本，写明回滚流程。
- 加版本握手 + capability probe + CLI 输出契约测试；
  不兼容时**拒绝启动并给出可操作错误**，不要运行中静默错乱。

---

### T3.2.1 泰语 code-switch：给 whisper 加 initial prompt  `DONE`（2026-09-19）
- **实测依据**：[`docs/data/thai-corpus-arm-2026-09/RESULTS.md`](../data/thai-corpus-arm-2026-09/RESULTS.md)
- 用**已经在发的** `terms.json` 当 whisper 的 `--prompt`：
  夹英文 CER **31.1% → 18.4%**，英文词命中 **8% → 51%**，纯泰语 3.9% → 3.1%。
- ⚠️ **必须带长度护栏，拐点已测**（2026-09-04，见 RESULTS.md §二）：
  **收益可复现的区间是 20–40 词**（三格都是 2.2%，30 附近最好）；
  54–70 词和基线区分不开且不单调（4.2% / 3.1%），**这份数据判不了**；
  85 词明确变差，100 词时连纯泰语都从 3.9% 崩到 22.2%。
  **落地时截到 40 词**——留余量，因为 `terms.json` 是用户会自己编辑的文件，
  只会越来越长。不设上限的话，某天用户多加几十个术语，泰语会毫无征兆地整个变差。
- **不要动 `-bs 1 -bo 1`**：实测换默认 beam search 只值 2 个词、0.3 个百分点。
- **✅ 已落地**：`Asr::new` 新增 `data_root` 参数并存下来（`Option<PathBuf>`）；
  `transcribe_thai` **每次转写现算** `crate::engine::latin_context_from_terms`
  （复用 T3.4 给 Qwen3-ASR 写的那份提取逻辑——**不是重新实现**，两个 ASR
  后端现在共用同一份「取 terms.json 的拉丁词、40 词封顶」的代码，同一个
  上限、同一份 40 词护栏的实测依据）；非空时传 `--prompt`。
- ⚠️ **一轮 codex 评审抓出一个真问题，已修**：最初的实现在 `Asr::new`
  构造时就把 prompt 算好缓存成字符串，导致用户运行期间编辑 `terms.json`
  要等进程重启才生效——跟 M2 纠错「每次都重新读术语表，改完下次录音即
  生效，不用重启」的既有承诺不一致。改成只存 `data_root`，`transcribe_thai`
  每次现读现算；多的开销是一次小文件读取，相对一次上秒的 whisper 转写
  可以忽略。
- ⚠️ **"D 方案按 3 遍复核"这条前置检查没能按原计划做**：原始 ARM 语料的
  音频文件不在这台机器上（只有 `manifest.txt`/`reference.tsv` 这些文字记录
  进了仓库，原始录音没有），没法重跑当年的 CER 测量。**改用能做的验证**：
  - `say -v Kanya` 合成 3 条不同的泰英混合句子（跟原语料完全无关的新样本），
    对同一句话分别用 `whisper-cli` 跑「不带 `--prompt`」和「带 `--prompt`」
    两遍直接对比输出。三句里两句能看到明确改善（"Docker"/"container"/"API"
    从被音译改为保留拉丁书写），一句只有部分改善（"API" 被救回、"bug"/
    "unit test" 没有）。⚠️ **这条最初的解释是错的，一轮 codex 评审挑出来
    的**：我原来写"覆盖不到的词救不回来，跟 RESULTS.md 一致"，但 RESULTS.md
    的原话恰恰是"救回了 20 个词，**包括表里没有的词**"——两处描述的其实是
    不一样的模式（第一句：连不在词表里的词也被救回，符合"语域提示"的说法；
    第三句：只有在词表里的"API"被救回，更像字面查表）。**老实说**：3 个
    临时样本谁的模式更能代表真实情况，判不出来，这就是样本量太小的正常
    表现，不该硬套一个解释让两边看起来一致。
  - 同一条样本连续跑 3 次（贪心解码，`-bs 1 -bo 1` 本来就是确定性的），
    逐字节输出完全一致——**没有采样噪声**，但这验证的是"贪心解码可复现"，
    不是"CER 数字可复现"，两者不是一回事。
  - **老实说**：这不等于把 RESULTS.md 里 31.1%→18.4% 那组数字重新坐实了一遍，
    只是确认了机制在新样本上仍然工作、没有因为这次改动而失效。真要重新
    核实原始 CER 数字，需要拿到 ARM 那台机器上的原始录音，这不是本次能做的。
- **代码验证**：
  - `new_stores_the_data_root_for_later_per_call_lookup` / `new_with_no_data_root_is_not_a_panic`：
    构造时的接线（存对了 `data_root`，`None` 不崩）。
  - **`prompt_actually_changes_real_whisper_output`（`#[ignore]`，真实
    端到端，不是 mock）**：这条是 codex 评审点名要求的——原来的两条测试
    只证明"字段算对了"，没证明"子进程真的收到了 `--prompt`"。新测试真的
    调 `whisper-cli` + 真实泰语模型 + `say` 现场合成的音频，对比带/不带
    `--prompt` 的两次转写，断言输出不同、且带 prompt 那次救回了至少一个
    技术词。为了可复现，**没有用这台机器上真实的 `~/.agentear/terms.json`**
    （开发者自己会编辑的文件，内容不受控），造了一份内容已知的临时表。
    手动跑：`AGENTEAR_VENDOR=<主 checkout>/vendor cargo test -- --ignored
    prompt_actually_changes_real_whisper_output`（worktree 里 `vendor/`
    被 gitignore，要指到主 checkout）。
  - `cargo test` 294 passed（292→294，不含 `#[ignore]` 的端到端测试）。

### T3.2.2 泰语模型复评（三方横比）  `BLOCKED`
- **卡在模型文件**：ADR-0004 的另两个候选（Thonburian medium /
  typhoon-whisper-turbo）**已经不在机器上**，要重跑 `scripts/build-thai-model.sh`。
- 在跑完之前，「换泰语模型也修不了 code-switch」**是推断不是结论**
  （依据：泰语本身已 3.9%，且失败是这一类模型的共性——训练集都是泰文转写）。
- 跑的时候必须**分组算 CER**（对照组 vs 两个 code-switch 组），不要给总分。

### T2.4.8 索引一致性判据补齐  `TODO`
- PR #34 评审的变异矩阵指出三格没人接住：
  1. `--reindex` 末尾的 FTS5 `'rebuild'`（评审给了可用的测试，红绿两侧都验过）
  2. `--search` 的 `created` 按字符截断（防 panic）——在 CLI 路径上，
     需要真重构可测性，不是浅抽取能接住的
  3. `rebuild` 抢 `kb/.lock`——要起两个线程真抢锁
- 1 可以直接抄；2/3 需要单独设计，不要浅抽取（上个 PR 实测过接不住）。

### T2.4.6 重新分类已有 routes  `TODO`
- **缺口**：`--replay-kb` 按 `routes/` 里**已有的**标签重放，不重新分类。
  所以在启用理解层之前落成 `unknown` 的记录，后来启用了也捞不回来。
- 需要一条单独的 `--reclassify-routes`：读 `derived/transcripts/`，
  重跑分类，更新 `routes/`，再让 `--replay-kb` 投递。
- **不要偷偷塞进 replay 里**：重放的语义是「按记录重建」，
  重新分类会改记录本身，那是两件事。

### T2.4.7 泰语的显式标记  `TODO`
- `LEADERS` / `SPOKEN`（`src/label.rs`）只有中英文，**没有泰语**。
  ASR 认得泰语，但泰语用户说「นี่คือไอเดีย」不会被识别成显式标记。
- 卡在**语料**：我没法判断泰语里哪些说法自然、哪些会误伤正常句子。
  需要一个泰语母语者给出七类标签的常用说法，再照中文那套加边界判据和测试。

### T2.4.5 OpenKnowledge  `DEFERRED`
- 见 [ADR-0006](../decisions/0006-openknowledge-as-personal-frontend.md)：
  它是编辑器不是服务，对当前定位没有增量能力，**现阶段不做**。
- 真要做的时候规则是：可以在文档里推荐，**不 fork、不 vendor、不进 bundle**
  （GPL-3.0 vs 我们的 Apache-2.0，单向不兼容）。
