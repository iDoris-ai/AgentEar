# AgentEar Roadmap — Milestone → Feature

> 「未来要做什么」。具体怎么做+验收见 [`tasks.md`](tasks.md)。
> 建立 2026-09-02，**M3 于 2026-09-08 重定义**（ADR-0007），本文 2026-09-14 更新。
>
> **编号沿用项目自己的里程碑**（`docs/milestones.md` 的 M0–M5），不另起一套。

## 已完成（不在本轮范围）

- **M0 ASR 基准验证** — 四模型横比，选定 SenseVoiceSmall q8（ADR-0001）
- **M1 按键即录即转写** — 验收 8/8，v0.3.1 已发布。含三语界面与泰语识别

## M2 — 理解与路由  ✅ 已完成并发布

目标：转写之后接上理解层。让文字**准确**（术语不被识别错），并且**知道自己是什么**
（idea / task / note …），据此分流到 `routes/`。

- **F2.1 项目术语表** ✅ — 给模型一份本项目固定词汇清单，而不是每次从上下文猜。
- **F2.2 标签识别与路由** ✅ — 重新定义 8 类边界后再实现；显式标记优先于模型推断。
- **F2.4 让 M2 真的能用** ✅ — 边车生命周期（连接优先、拉起兜底）、
  `routes/` 下游投递到 `kb/**/*.md`、L2 全文索引。
  发布轨迹：**v0.4.0 → v0.4.1 → v0.4.2**。

## M3 — 实时语音对话（**方案已重定义，见 [ADR-0007](../decisions/0007-realtime-voice-architecture.md)**）

> ⚠️ 本节 2026-09-08 被重写。原来的 M3 只是「语音输出与打断的可行性摸底」；
> jason 当天口述把产品形态说清了一层：**打电话式、可打断、实时的语音对话**，
> 中英泰可配置且过程中随时切，全本地。摸底那一版已完成，现在是实现阶段。

**产品分两档（这是产品决策，不是妥协）**：

| 档 | 形态 | 内存门槛 | 状态 |
|---|---|---|---|
| **V1** | 打断式半双工（目标形态：VAD 检出开口就掐掉 TTS；**现状：推键式**） | ≤10 GB | **M3 目标，链路已跑通但未收口** |
| **V2** | 实时全双工（边听边想） | 64 GB 级 | **未排期**，不占里程碑编号 |

V2 的触发条件是「出现经验证覆盖中文和泰语、且在 Apple Silicon 上达到实时的
本地全双工模型」——今天不成立，所以排期无从谈起。
**V1 不是 V2 的临时替代品，是长期保留的低配档位。**

⚠️ **V1 至今落地的打断入口是「推键式」**（按录音键先掐掉正在播的回答再开麦），
**不是 VAD 检测到开口就掐**——ADR-0007 §3 表里那句写的是目标形态，**还没实现**。
AEC 那格也没解决（VPIO 内容级 0/5、**事件级 5/5**）。
**「自触发率 = 0 / 端到端打断 <300 ms / 误打断 <1 次每 10 分钟」三条都未验收。**

- **F3.1 TTS 方言可行性** ✅ — 已出 ADR-0005 草稿。
- **F3.3 TTS 模块（三语，派给 Arm）** — 接口先定死（`POST /speak` → `audio/wav`），
  **第一档**用 macOS 内置 `say` 保底、**第二档**用 VoxCPM2。
  ✅ **2026-09-14 实况：第二档已成默认后端**（`mlx-community/VoxCPM2-4bit` 经
  `mlx-audio`，48 kHz，`--backend voxcpm2`），**`say` 降级为保底**；
  Rust 侧已接（`talk_enabled` 打开时才会念），但**人耳验收仍没做**。
  ⚠️ 这两档是 **TTS 的实现档位**，和上面的产品 V1/V2 双工档位**是两回事**。
- **F3.4 实时语音对话 V1** — 原定顺序：
  `T3.4.0 可行性 spike（跑完，**判定不通过但方案不推翻**）→ T3.4.1 引擎适配层 ✅（v0.5.0）
  → T3.4.2 通话会话层 → T3.4.3 mock LLM → T3.4.4 serve/mcp 集成接口`。
  🔄 **2026-09-14：链路已跑通**——T3.4.2 的**编排部分已落地**（选 A 自主编排，
  `src/session.rs`；引擎适配 `src/talk.rs`），**T3.4.3 已 DONE**（中英泰三语端到端 + 回环验证）。
  ⚠️ **但 T3.4.2 没 DONE**：AEC 事件级、VAD 自动打断、三条出口判据、
  `StreamCheckpointPolicy`、通话入口 UI 都还没有。**◀ 当前**。
- **F3.5 选型补课**（与 F3.4 并行，不阻塞）— 中英 ASR 横比、whisper 逐条 CER 入库、
  逐组件许可证表、方言人耳验收、上游版本锁定。
- **T3.2.1 泰语 code-switch** — 独立于 F3.4，约 10 行改动，收益已实测。

## 后续（本轮之后，顺序未定）

- **M3.5 录音笔能力刻画** — 机器到手即做，不排队
- **M4 录音笔接入** — 依赖 M3.5 的结论
- **输入侧方言** — 闽南语听力缺口。要动就得推翻「ASR 侧仍按原标准」那条约束，
  需 jason 单独拍板，**不在本轮**

---

> **当前聚焦：M3 / F3.4 —— 通话链路已跑通（未发版），下一件事是 T3.4.2 的出口判据收口。**
> 欠的是硬的：AEC 事件级自触发、VAD 自动打断、端到端打断 <300 ms、误打断率、
> `StreamCheckpointPolicy`、通话入口 UI。**在此之前不要把 M3 写成已完成。**
> F3.5 的选型补课（含 T3.5.4 TTS 人耳验收）与 T3.2.1 可并行（互不依赖）。
> `docs/milestones.md` 的 M3 已同步为「实时语音对话」并含 V1/V2 分档；
> 但它的 §「未验证前提」还没跟上 T3.4.0 的实测，以 `benchmarks-m3.md` §7 为准。
