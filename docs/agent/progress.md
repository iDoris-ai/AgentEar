# 仓库实时状态

> 此刻在做什么、卡在哪、分支与 PR。每推进一步就更新，宁可啰嗦不可与仓库脱节。

**更新时间**：2026-09-02（T2.1.1 codex 评审后）
**当前分支**：`feat/T2.1.1-terms-table`（worktree `../AgentEar-F2.1`）
**main HEAD**：2ccb538 — feat(m2a): 技术术语纠错（PR #9 已合并）

## 此刻状态

**已完成**：T2.1.1（PR #11 已合并）
**进行中**：T3.1.1 TTS 候选调研（F3.1）—— 纯文档

## 环境前置（开工前必须确认）

M2 的所有 task 都依赖 LLM 边车在跑：

```bash
scripts/serve-llm.sh          # 默认 127.0.0.1:8793
./target/release/agentear --diagnose | tail -6   # 确认「✅ 服务」
```

模型已在 `~/.agentear/llm/models/ornith`（7.8 GB）。环境不在了就先跑
`scripts/setup-llm.sh` 重建。

⚠️ **边车没起时 F2.1/F2.2 的验收命令会失败**，那是环境问题不是代码问题，
先查这一条再怀疑实现。

## 待决问题（需要 jason 拍板，不擅自决定）

| # | 问题 | 阻塞谁 | 现在怎么办 |
|---|---|---|---|
| **Q3** | **`~/Desktop/agentear-tts-samples/` 里的三段音频，`nan_tw` 和 `yue_hk` 到底是不是闽南语/粤语，还是用普通话念的汉字？** | T3.1.3 的全部结论 | **需要 jason 亲耳听。** 我无法播放音频，而这一条决定 M3 能不能做方言 |
| Q1 | 「今天开会讨论了传输协议」该判 note 还是 journal？spec.md 按 journal 定的，但这是产品决策 | T2.2.1 的验收 | 先按 spec.md 实现，把这条列为待确认，不当定论 |
| Q2 | 若闽南语本地 TTS 确实没有可用方案，M3 怎么走（只做普通话 / 推迟 / 其他） | T3.1.3 | 调研出结论后带着事实来问，**不替他选** |

## 已完成（本轮之前）

- M1 全部，v0.3.1 已发布（三语界面 + 泰语识别 + 按需下载）
- M2a 术语纠错（PR #9）—— 它暴露的问题正是 F2.1 的立项依据

## 分支与 PR

| Task | 分支 | PR | 状态 |
|---|---|---|---|
| T2.1.1 | `feat/T2.1.1-terms-table` | #11 | **DONE** |
| T3.1.1 | `docs/T3.1.1-tts-survey` | 待开 | PR_OPEN |

worktree：`../AgentEar-F2.1`、`../AgentEar-F3.1`、`../AgentEar-plan`（后者已合并，待人工清理）

## 跟进账本

3 条 OPEN（FU-1/2/3，全部来自 T2.1.1 的 codex 评审）。按 pilot 规矩，
**主线 task 全部做完才批量清**，不提前做。

## 踩过且已钉住的坑（给后面的 task 看）

1. **worktree 里没有 `vendor/`**（不入库）。跑真实转写要
   `AGENTEAR_VENDOR=/Users/jason/Dev/tools/AgentEar/vendor`。
2. **提示词的格式歧义会让整个功能失灵**，不是错一条。T2.1.1 踩了五版：
   写「可能被识别成 X」模型会反向替换；写「遇到左边替换右边」会误伤真实词；
   光写「由上下文决定」不够，要 few-shot 反例；最后还得把本身是高频普通词的
   alias（`ID`/`肉`/`日报`）整个去掉。
3. **改提示词必须真调一次边车验证**，单元测试和真实录音都可能全绿而功能是错的。

## 变更日志

- 2026-09-02 建立 `docs/agent/` 七件套规划，覆盖 F2.1 / F2.2 / F3.1
