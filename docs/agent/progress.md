# 仓库实时状态

> 此刻在做什么、卡在哪、分支与 PR。每推进一步就更新，宁可啰嗦不可与仓库脱节。

**更新时间**：2026-09-03（本轮规划全部交付）
**当前分支**：main（干净）
**main HEAD**：`2b3b916` — fix(T2.3.4) last-good 落盘

## 此刻状态：**规划内的 task 全部完成，等 jason 拍板才能继续**

pilot 的三条交付条件全满足：无 READY task、无未合并 PR、跟进账本清空。

本轮做完 **14 个 task**（F2.1 术语表 5 个、F2.2 标签路由 5 个、
F3.1 TTS 摸底 3 个、F2.3 账本提升 4 个），合并 **21 个 PR**，
测试从 8 条到 **148 条**。

## ⚠️ 但「交付条件满足」不等于「工具能用」

这一条必须写在最前面，否则接手的人会以为 M2 已经可用：

| | 状态 | 用户装上能用吗 |
|---|---|---|
| M1 输入法（按右 Command 说话上屏） | ✅ | **能**，v0.3.1 |
| 泰语识别 | ✅ | **能**，菜单里选、自动下模型 |
| **M2 术语纠错 / 标签 / routes** | 代码完成 | ❌ **不能** |
| M3 语音输出 | 只有调研 | ❌ 没有 |

**M2 的 3421 行代码全在 main 上，但最新发布版 v0.3.1 一行都不包含。**
就算从源码构建，要用术语纠错还得：跑 `setup-llm.sh` 下 7.8 GB →
另开终端挂着 `serve-llm.sh` → 回菜单勾选。**那不是工具，是开发环境。**

`routes/` 目前**只写不读**：标签识别的结果落盘了，下游什么都没有。

## 待 jason 拍板（三条 BLOCKED，见 tasks.md 的 F2.4）

| # | 问题 | 为什么我不能替他决定 |
|---|---|---|
| **T2.4.1** | 守护进程要不要拉起/守护边车？ | 改变 ADR-0002 划的线——那里说边车「独立进程、可独立重启、独立崩溃」，自动拉起意味着守护进程要管它的生死。**架构决策** |
| **T2.4.2** | 现在发 v0.4.0，还是等 T2.4.1？ | 现在发的话用户看到开关但点了没用（边车没起），比不给开关更困惑。**产品决策** |
| **T2.4.3** | routes 的下游先做投递还是别的？ | 取决于他想先看到哪个价值。**产品决策** |

## 其他待决问题

| # | 问题 | 阻塞谁 | 现在怎么办 |
|---|---|---|---|
| **Q3** | `~/Desktop/agentear-tts-samples/nan_tw_000.wav` 是闽南语，还是普通话念的汉字？ | ADR-0005 的全部结论 | **需要人耳。** 我无法播放音频，这一条决定 M3 能不能做方言 |
| Q1 | 「今天开会讨论了传输协议」该判 note 还是 journal？ | 标签定义的权威性 | 实现按 journal 做了，标为临时决策，未当定论 |
| Q2 | 若闽南语 TTS 确实没方案，M3 怎么走 | T3.1.3 的结论 | ADR-0005 已按两种情形写好，等 Q3 |

## 环境前置

M2 的功能和它们的测试都依赖 LLM 边车：

```bash
scripts/setup-llm.sh          # 首次：装环境 + 拉 7.8 GB 模型
scripts/serve-llm.sh          # 每次：起服务，默认 127.0.0.1:8793
./target/release/agentear --diagnose | tail -6   # 确认「✅ 服务」
```

⚠️ 边车没起时，`cargo test --release -- --ignored` 的三条集成测试会失败——
**那是环境问题不是代码问题**，先查这一条再怀疑实现。

## 踩过且已钉住的坑（给后面的 task 看）

1. **worktree 里没有 `vendor/`**（不入库）。跑真实转写要
   `AGENTEAR_VENDOR=/Users/jason/Dev/tools/AgentEar/vendor`。
2. **提示词的格式歧义会让整个功能失灵**，不是错一条。术语表踩了五版：
   写「可能被识别成 X」模型会反向替换；写「遇到左边替换右边」会误伤真实词；
   光写「由上下文决定」不够，要 few-shot 反例；最后还得把本身是高频普通词的
   alias（`ID`/`肉`/`日报`）整个去掉。
3. **改提示词必须真调一次边车验证**——单元测试和真实录音都可能全绿而功能是错的。
4. **模型输出的结论至少连跑 3 次**。「术语表修好了长文」和「标签 18/18」
   都曾因单次通过而下早了，后来被推翻。
5. **`env -u HF_ENDPOINT`** 是本机 huggingface_hub 报 `LocalEntryNotFoundError`
   的通用绕法（踩了三次才总结出来）。
6. **bash 3.2 会把 `$VAR` 后紧跟的中文标点当成变量名**。改完 `scripts/*.sh`
   跑 `scripts/lint-shell.sh`。

## 分支与 PR

无 open PR。以下 worktree 已合并，**待人工清理**（pilot 只列不删，
因为 `git worktree remove` 是文件系统删除，会连 gitignore 的文件一起抹掉）：

```bash
for w in plan F2.1 F2.2 F2.3 F3.1 followups wrap; do
  git worktree remove ../AgentEar-$w 2>/dev/null
done
```

## 变更日志

- 2026-09-02 建立 `docs/agent/` 七件套规划
- 2026-09-03 F2.1 / F2.2 / F3.1 / F2.3 全部完成，账本清空，
  三条「让 M2 能用」的 task 标 BLOCKED 等拍板
