# 同一模型、同一台机器、同一份语料的重复测量
# 目的：给 ADR-0004 §3「单次测量，不足以建立稳定排名」这句话一个量。
#
# 重算：scripts/bench-thai.sh <ggml-distill-q5_0.bin>   连跑三次
# 机器：MacBook Pro M1 Max / 64 GB，macOS 25.4.0
# whisper.cpp 7de8dd78（与入库那次相同），线程 4，语料 10.6s / 32.2s / 97.7s
# 模型 sha256(12) = 5bfc04f1931a（与入库那次相同，逐字节同一份产物）

来源                    体积MB   短RTF   中RTF   长RTF   短RSS   长RSS
入库 (2026-08-22)          574   0.098   0.208   0.186     694     712
今天 (1/3)                 574   0.103   0.154   0.142     694     713
今天 (2/3)                 574   0.104   0.213   0.165     694     713
今天 (3/3)                 574   0.102   0.185   0.184     694     712

# ⚠️ 本文件是**人工汇总**，不是脚本原样输出（脚本每次只打一行）。
#
# 读出来的东西 —— 口径一律是**同一长度列内**的 最大/最小：
#
#   长度档   六个模型之间(入库表)   本模型重复三次
#   短        1.37×                 1.02×   0.102–0.104  中位 0.103  CV 1.0%
#   中        1.40×                 1.38×   0.154–0.213  中位 0.185  CV 16.0%
#   长        1.30×                 1.30×   0.142–0.184  中位 0.165  CV 12.9%
#
#   RSS      逐 MB 复现（694 / 712–713），可以当稳定量用
#
# 中档与长档：一个模型自己重复三次的波动，和六个模型之间的差距一样大 ——
# 那两列的模型排名在重复取中位数之前不成立。
# 短档不是：模型间 1.37× 远大于本模型的重复波动 1.02×，解释不成噪声。
#
# ⚠️ n=3，且只测了一个模型。这证明不了别的模型有同样的波动分布。
#    准确的范围是：「中/长档的单次 RTF 不能当稳定点估计」+
#    「原表没有重复测量，不宜作确定排名」。
# ⚠️ 跨长度档取极值算「最快到最慢接近两倍」是不成立的 —— 上面每格都在同列内比。
#
# 入库那行的 0.208 落在今天三次的范围内，**现有数据没有显示它异常**；
# 但历史只有一个点、今天只有三个点，判断不了两天的分布有没有差异。
# （第一次只跑一遍看到 0.154，差点误读成「今天快了 26%」。）
